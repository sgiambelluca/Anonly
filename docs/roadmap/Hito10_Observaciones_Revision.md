<!-- CONTEXT: scope=roadmap-hito10-observaciones | dependencias=roadmap/MVP.md,adr/ADR-036-Auditoria-Pre-Hito10-React-Client-Workers.md,adr/ADR-037-Zoom-Rerender-RenderRequested-Scale.md,adr/ADR-038-Reanalisis-Parcial-Preservando-Ediciones.md,adr/ADR-044-Preview-Grupos-Mediacion-Orchestrator.md | audiencia=humanos | fase=10 -->

# Hito 10 — Observaciones de revisión (no bloqueantes) y ambigüedades

> Documento vivo, actualizado PR por PR durante la implementación del Hito 10. Recoge todo lo que el revisor o el implementador señalaron como **no bloqueante** (no impidió aprobar el PR) pero que amerita que el humano lo revise en algún momento: decisiones de scope, limitaciones estructurales conocidas, tensiones entre secciones del spec, y ambigüedades resueltas sin improvisar. No es un changelog de lo que se hizo — para eso están los mensajes de commit — es un registro de **lo que falta decidir o revisar**.
>
> Convención: cada entrada cita el PR donde se originó, el archivo/sección relevante, y si tiene una tarea de seguimiento asociada (tasklist de la sesión) o no.

---

## PR1 — Scaffold `apps/react-client`

- **Stores creados en PR1, no en PR5 (tabla ADR-038 §8)**: los 6 stores de Zustand (`document`, `entities`, `rules`, `pipeline`, `viewer`, `settings`) se implementaron como placeholders locales en el scaffold, aunque la tabla canónica de PRs los asigna al PR5 (`core-adapter`). Confirmado como intencional por el revisor (son placeholders puros, sin conexión al bus) y por el humano al aprobar. Solo una nota de trazabilidad, sin acción pendiente.
- **Ícono del Hero**: 40px (`h-10 w-10`) vs. 32px que documenta `Components.md` §11. Cosmético, sin corregir.
- **`console.warn` en `settings.store.ts`**: usado para fallos de `localStorage`. Válido hoy (permitido en `apps/`); candidato a moverse a un logger de UI si se crea uno más adelante.

## Auditoría pre-Hito-10 (ADR-036) + reapertura (ADR-037/038)

- **Q1 (ADR-038)**: al desactivar NER, un grupo cuyos únicos members eran NER se **elimina** aunque el usuario lo haya editado (no se conserva "huérfano deshabilitado"). Confirmado por decisión de diseño, no revisitado.
- **Q2 (ADR-038)**: los índices de placeholder (`[PERSON 03]` → `[PERSON 05]`) pueden correrse tras un `reanalyze`, visible para el usuario. Aceptado como costo del determinismo de ADR-028.
- **Q3 (ADR-038)**: `performancePreset` cambiado con documento abierto no aplica en caliente — se persiste y aplica recién al próximo documento. Aceptado.

## PR2 — Grouping re-análisis (`reopenSession`/`dropOccurrences`)

- **Limitación estructural real, sin tarea de seguimiento todavía**: `dropOccurrences` solo cubre el caso "grupo eliminado" para descartar conflictos stale (`03_Data_Model.md` §13.25). El caso "el grupo sobrevive pero un `candidate` puntual referenciaba una ocurrencia eliminada" **no se puede detectar** con el contrato actual de `ConflictCandidate` (`shared/src/types.ts`), que no tiene `occurrenceId` ni `bbox`. Ampliarlo requeriría ADR + cambio de `Contracts.md` (R-2/R-19). No corrompe datos (deja un conflicto "stale" en el snapshot), pero el spec (§13.25 / ADR-038 §2) describe el caso completo sin la salvedad de esta limitación — **conviene que un futuro PR de docs acote la redacción del spec a lo que es estructuralmente posible, o abra el ADR de ampliación si de verdad hace falta**.
- `ENTITY_GROUP_UPDATED.changes` incluye siempre `["members","aliases"]` en un `dropOccurrences`, aunque `aliases` a veces no cambie de contenido. Sobre-emisión conservadora, inofensiva (evento idempotente). Sin acción.
- Dedup por identidad es O(n²) en `recordedOccurrences` — mismo patrón que `findOverlapConflict` ya existente. Anotado por si el perfilado futuro lo señala; no es una regresión de este PR.

## PR3 — `Orchestrator.reanalyze`

- **Gap real, sin tarea de seguimiento formal todavía (mitigado a nivel UI en PR6)**: un patch combinado `{ner, ocr}` en una sola llamada a `reanalyze` no logra la equivalencia con una corrida fresca en dos sub-casos (ocurrencias NER huérfanas al combinar OCR+NER-off; NER incompleto —solo páginas re-OCR— al combinar OCR+NER-on). No hay bug de crash ni de tipos, pero el resultado puede quedar inconsistente. `ADR-038 §5.4` no detalla estos sub-casos. **Mitigado en PR6**: `SettingsDialog` nunca envía un patch combinado — emite dos llamadas secuenciales (OCR primero). La mitigación evita el síntoma pero el gap de `Orchestrator.reanalyze` en sí sigue sin resolver a nivel de Core; si algún consumidor futuro (o un test de integración) llama `reanalyze` con ambos campos a la vez, el problema reaparece.
- Guard defensivo `patch == null` en `validateReanalyzePatch`: inalcanzable vía la firma tipada, inofensivo. Sin acción.

## PR4 — `render-engine` zoom (`RenderRequested.scale`)

- **Bug real, con tarea de seguimiento abierta (bloqueando PR9/Export)**: `pendingRenders` (mecanismo de supersede de renders obsoletos) nunca se limpia al completar un render con éxito — solo en `dispose`/`unloadDocument`/`loadDocument` (reload). Combinado con que la clave de supersede no incluye `mode` (decisión literal de ADR-037 §4), una entrada de un preview a `previewScale` que ya terminó puede cancelar espuriamente un export posterior en `mode:"full"` de la misma página. Intenté un fix rápido ("limpiar la entrada al completar") pero reintroduce una carrera distinta con el propio mecanismo de supersede-en-cola (rompe el test `superseded render in queue is discarded`). El fix correcto necesita distinguir invocaciones vía `handleRenderRequested` (deben participar del supersede) de invocaciones directas del Orchestrator —export/OCR— (deben ser inmunes). Documentado en el header de `render.engine.ts`. **Debe resolverse antes de que el PR9 (Export) pueda considerarse completo** — el bug es 100% reproducible en el flujo normal "usuario previsualiza con zoom, después exporta".
- Cosmético: `pending.controller.signal.aborted` en `throwIfSuperseded` siempre evalúa `false` en la práctica (código muerto, el `.abort()` solo afecta a la entrada reemplazada, no a la vigente). Sin acción, señalado por el revisor como cleanup opcional.
- Ventana angosta: un delta-render (`GROUP_TOGGLED`/`GROUP_REPLACEMENT_CHANGED`) que corre mientras hay un zoom en vuelo a otra escala puede ser superseded incorrectamente, perdiendo visualmente el cambio de grupo hasta el próximo evento. Misma raíz que el bug de arriba; se resuelve junto con el fix de supersede.
- **Resolución (2026-07-19, las tres observaciones)**: el supersede quedó acotado al flujo por eventos — `handleRenderRequested` despacha por la vía interna `renderPagesInternal`/`renderPageInternal` con `participatesInSupersede = true`; las invocaciones directas de `renderPage`/`renderPages` (export, tests) y el delta render pasan `false` y nunca consultan `pendingRenders`. Las entradas no se limpian al completar (deliberado: limpiar reintroduce la carrera del render en cola superado; una entrada residual es inofensiva e invisible para la vía directa) y el `AbortController` muerto se eliminó (detección por comparación de escala pura). Sin cambio de contrato público ni de la clave de ADR-037 §4. Tests de regresión: `direct full render (export) ignores supersede entry...` y `delta render ignores supersede entry...` en `edge.test.ts`; los dos tests de supersede del PR4 pasan sin modificación. Spec: `Render_Engine.md` v1.3.1 (§8, §13 caso 21, §14). Desbloquea el cierre del PR9 (Export).

## PR5 — `core-adapter`

- **Gap conocido, heredado por PR6/PR7**: `initCore(config?)` no deriva un `EngineConfig` desde `settings.store` — hace passthrough directo a `createCore`. Motivo: `Partial<EngineConfig>` es shallow (si se provee `workerPool`, TypeScript exige un `WorkerPoolConfig` completo con 11 campos, varios sin defaults consolidados en un único doc que el cliente pueda reproducir sin inventar valores). Consecuencia práctica: hoy, `nerEnabled`/`ocrLanguages`/`performancePreset` persistidos en `settings.store` **no tienen ningún efecto real** sobre el Core hasta que algún PR futuro cablee `buildEngineConfig(settings)` en el boot de `App.tsx` (probablemente cuando se agregue `settings.load()` al arranque). No es un bug vivo hoy porque `settings.load()` tampoco está cableado (todo corre con los defaults de `EngineConfig`, que coinciden con los defaults de `settings.store`).

## PR6 — Toolbar y diálogos de flujo

- **Bug menor, sin tarea de seguimiento**: en `SettingsDialog.handleConfirmReanalyze`, el store de settings se actualiza (`applyToStore`) **antes** de que las llamadas secuenciales a `reanalyze` resuelvan. Si la primera (`ocr`) rechaza, el store ya quedó persistido con ambos cambios pero el patch de `ner` nunca se envió — un segundo click en "Reanalizar" no-opea silenciosamente porque el diff ya da vacío. Se corrige aplicando el store recién tras el éxito de ambas llamadas, o reseteando el diff en el `catch`.
- Botón "Reintentar" genérico no implementado — correctamente, no existe API (`IPipelineOrchestrator`) que lo respalde sin inventar algo no documentado.
- `OCR_PAGE_FAILED` no wireado a ningún componente — hereda el gap del bus-bridge de PR5 (el evento no muta ningún store).
- El banner "Desactivar NER y reanalizar" (recuperación de `NER_MODEL_MISSING`) llama `reanalyze` pero no actualiza el toggle de `settings.store.nerEnabled` — quedaría desincronizado visualmente, sin efecto real hoy dado el gap de PR5 de arriba.
- Tensión entre `Components.md` §2.1 (que agrega el matiz "+ si hay jobs remanentes" a cuándo mostrar `CancelButton` en stage `Ready`) y §2.4 (regla propia, sin ese matiz): no hay campo en `pipeline.store` que represente "jobs remanentes", así que se implementó solo la regla de §2.4. Vale la pena reconciliar ambas secciones en un futuro PR de docs.

## PR7 — Visor

- **Rechazado por el revisor y corregido en la misma sesión (re-revisión: APPROVED)**: los dos `PdfViewer` (original/anonimizado) no sincronizaban el scroll real (solo compartían `visibleRange`, lo que desmontaba páginas del visor no scrolleado). Se agregó `scrollSync.ts` (`computeScrollSyncTarget`) cableado en `PageVirtualizer` vía la nueva prop `scrollToPageIndex`. El revisor trazó la cadena completa (incluido el corte del loop de realimentación) y confirmó que converge sin re-render infinito.
- Sincronización por **página**, no por píxel: en scrolls parciales los dos paneles pueden diferir visualmente hasta ~1 página. Consistente con el diseño (sincroniza vía `currentPageIndex`, no offset exacto) — no es un defecto, solo una característica a tener presente.
- Edge teórico con `pageSize` fraccional (zoom no entero): redondeo de `scrollTop` podría dejar un desfase estable de 1 página entre los dos visores. Acotado (no hay loop infinito), caso límite de QA, no bloqueante.
- `viewer.store.sideBySide` (`React_Client.md` §3.5, default `true`) no tiene setter ni ningún componente lo lee — el fallback mobile/desktop de `SideBySideViewer` es una media query CSS pura, no condicionada por ese campo. Campo inalcanzable con el contrato actual; nadie especifica qué UI debería togglearlo. Señalado para que el planificador confirme si un futuro PR debe cablearlo o si el campo debería eliminarse del store.
- `Components.md` §5.3 / `07_Performance_Strategy.md` §3 piden un "pool de canvas reutilizables"; la implementación monta/desmonta `PageCanvas` por reconciliación de React (`key={pageIndex}`) en vez de un pool explícito de `<canvas>`. El objetivo funcional (solo ±1 página montada) se cumple; es una diferencia de la letra del spec, no del comportamiento observable, con ≤ ~6 canvases vivos en la práctica. Candidato a ajustar la redacción del spec en vez de el código.
- Doble emisión de `RENDER_REQUESTED` (uno por cada `PdfViewer`, mismo `pageIndices`/`scale`) cuando ambos reaccionan al mismo `zoom`/`visibleRange` global: inofensivo hoy (cache LRU + supersede lo absorben), pero **al resolver el bug de supersede del PR4 (arriba)**, conviene revisar si el `visibleRange` compartido entre dos `IntersectionObserver` sigue teniendo sentido o si debería pasar a ser por-`kind`.

## PR8 — Panel de Entidades + conflictos

- **`ConflictDialog` sin atajos "Usar Regex"/"Usar NER"**: `UX_Guidelines.md` §6 los menciona, pero `ConflictResolveRequested` (`Contracts.md`, `04_Event_System.md` §10) solo transporta `{documentId, conflictId, mode: ReplacementMode}` — no hay forma de expresar "qué fuente ganó" sin ampliar el contrato. Se implementó solo el flujo "Personalizado" (elegir un `ReplacementMode` real), confirmado como la única opción contract-consistente. Si se quieren los atajos literales, hace falta ADR + cambio de `Contracts.md`.
- **"Ver ocurrencias" (members con pageIndex+bbox+value) no implementado**: `OccurrenceRef` (`03_Data_Model.md` §8) no tiene campo `value`. Confirmado, no es un descuido.
- **Indicador "editado manualmente" (punto azul, `UX_Guidelines.md` §3.3) no implementado**: requeriría que la UI reimplemente la resolución de reglas de Grouping (fuera de su rol) para saber si el modo actual difiere del default. `EntityGroup` no expone ese dato.
- **`GroupContextMenu` implementado a mano, sin `@radix-ui/react-dropdown-menu`**: `Components.md` §13.7 pide que los componentes interactivos pasen por wrappers de Radix, pero agregar esa dependencia nueva requeriría ADR (P-9/R-12, prohibición absoluta sin ADR). Se priorizó no agregar la dependencia. Consecuencia: el menú a mano no tiene navegación por flechas ni gestión de foco como daría el primitivo de Radix. **Necesita una decisión del planificador**: ¿ADR para agregar `@radix-ui/react-dropdown-menu`, o aceptar formalmente el hand-roll?
- Estado vacío binario en `App.tsx` (`hasAnyGroup`): un documento cargado sin entidades detectadas sigue mostrando el estado "sin documento" en vez del mensaje específico de `UX_Guidelines.md` §11 fila 2 ("No se detectaron entidades..."). El dato para distinguir ambos casos existe (`pipeline.store.stage`), simplemente no se usó en este PR. Refinable en un PR posterior.
- Toasts de feedback (Merge/Split) y popover de aliases/edición inline de `canonicalValue` (`Components.md` §3.3/§3.6/§3.7): diferidos por no existir un componente `Toast` todavía en ningún PR previo. Correctamente fuera de alcance.

## PR9 — Panel de Reglas + Export

- **Cambio arquitectónico verificado, no una laguna**: `core-adapter/actions.ts` (`createRule`/`updateRule`/`deleteRule`) ahora muta `rules.store` directamente además de emitir el evento al bus. Confirmado por el revisor contra `04_Event_System.md` §6/§10 y `06_Pipeline.md` §11: los tres eventos `RULE_*` son estrictamente UI→Grouping, sin evento de vuelta Core→UI, y `bus-bridge.ts` no suscribe nada a `rules.store`. Consecuencia aceptada: si Grouping alguna vez rechazara una regla (hoy no lo hace), `rules.store` divergiría silenciosamente del Core — no hay evento de rechazo para reconciliar. Tradeoff inherente al diseño sin evento de retorno, aceptable para MVP; **revisar si algún ADR futuro agrega un evento de confirmación/rechazo de reglas**.
- `ExportDialog` omite `settings.store` pese a que `Components.md` §7.1 lo lista como dependencia — ningún campo de settings aplica hoy a `ExportOptions` (los defaults salen de `ExportConfig`). Deviation documentada; candidato a una nota de spec más que a un fix de código.
- `EXPORT_FAILED` se muestra como `Banner` dentro del `ExportDialog`, no como "toast" (letra de `React_Client.md` §8). Cosmético, ubicación natural dado que el flujo vive en el diálogo.
- `MIN_EXPORT_DPI = 1` vs. `Export_Engine.md` §9 ("dpi > 0"): equivalente en la práctica para DPI entero, el `Select` solo ofrece 150/300. Matiz menor.
- Estimación de tamaño de archivo del mockup de `UX_Guidelines.md` §8.2 no implementada — correctamente, no hay fórmula documentada que la sustente.

---

## PR10 — E2E base (7/7 bugs reales resueltos, PR10 cerrado)

Suite de Playwright creada (`tests/e2e/`: escenarios 1, 6, 8 de `07_Performance_Strategy.md` §11.3 + `support/fixtures.ts`, que genera los PDFs en memoria vía `tests/fixtures/generate.ts` sin commitear binarios). Typecheck y eslint verdes. Es la **primera vez que `pdfjs-dist` y `@huggingface/transformers`/`onnxruntime-web` corren de verdad en un browser real** — todos los tests unit/contract/edge de `pdf-engine`/`render-engine`/`ner-engine` mockean esas fronteras (ADR-021 §5). Destapó **siete** bugs reales, nunca detectables antes:

1. **`PDF_INVALID` al importar cualquier PDF** — `GlobalWorkerOptions.workerSrc` de pdfjs-dist nunca se configuraba en ningún lado del código. `getDocument()` rechazaba con `Error: No "GlobalWorkerOptions.workerSrc" specified.`, reclasificado por el catch-all de `pdf.engine.ts` (ADR-020 §4) como `PdfInvalidError`/`PDF_INVALID` genérico — indistinguible en la UI de un PDF realmente corrupto. **Resuelto** (commit `fix(react-client,ner-engine): dos bugs reales...`, 2026-07-22): `apps/react-client/src/main.tsx` configura el worker vía `import ... from "pdfjs-dist/build/pdf.worker.min.mjs?url"` antes de cualquier `getDocument()`; `pdfjs-dist` pasa a ser dependencia directa de `apps/react-client` (mismo `pdfjs-dist@4.10.38` ya resuelto en el workspace pnpm — confirmado que `GlobalWorkerOptions` es el mismo singleton que usan `pdf-engine`/`render-engine`, no una instancia duplicada).
2. **`NER_MODEL_MISSING` al llegar a la etapa de detección** — `ner.engine.ts` seteaba `env.allowRemoteModels = false` pero nunca `env.allowLocalModels = true` (default `false` en browser, `true` en Node — por eso los tests, todos en Node, nunca lo detectaban). Con ambos en `false`, `pipeline()` de `@huggingface/transformers` rechaza sincrónicamente con `"Invalid configuration detected: both local and remote models are disabled."` — mismo `NER_MODEL_MISSING` genérico en la UI. **Resuelto** (mismo commit): una línea (`env.allowLocalModels = true;`) en `configureTransformersEnv()`.
3. **`onnxruntime-web` no podía cargar su WASM desde `public/`** — `import()` dinámico ESM del glue `.mjs` (WASM threaded), rechazado por Vite por diseño (no importa módulos ES desde `public/`). **Resuelto vía ADR-039** (planificador, 2026-07-22): `NerConfig.wasmPaths?: string | NerWasmPaths` + `EngineConfigOverrides` (widening de dos niveles de `createCore`), assets movidos a `src/assets/onnxruntime/` (`assets.lock.json`), importados con `?url` e inyectados desde `core-adapter/index.ts`. Dos intentos previos descartados (headers COOP/COEP, `optimizeDeps.exclude`) confirmados inútiles y revertidos — quedó documentado que el error ocurre *antes* de necesitar `SharedArrayBuffer`.
4. **CSP bloqueaba el `import()` dinámico vía `blob:` que hace `onnxruntime-web` internamente** — tras el fix #3, `pipeline()` seguía rechazando con `TypeError: Failed to fetch dynamically imported module: blob:...`. La CSP original (`worker-src 'self'`, sin `script-src` explícito) no cubría el patrón de carga por blob URL. **Resuelto**: `script-src 'self' blob:` + `worker-src 'self' blob:` en el `<meta http-equiv="Content-Security-Policy">` de `index.html`.
5. **CSP bloqueaba `WebAssembly.instantiate()`** — con el fix #4 aplicado, el siguiente error fue `CompileError: ... violates ... "script-src 'self' blob:"` (falta el permiso de compilar WASM). **Resuelto**: agregado `'wasm-unsafe-eval'` (CSP Level 3, acotado a compilar/instanciar WASM — **no** el `'unsafe-eval'` completo) a `script-src`. Verificado con inferencia NER real en consola del browser (`classifier("Juan Pérez vive en Belgrano 1234...")` devolvió PER/LOC/ORG correctos). `docs/architecture/08_Security_Model.md` §3.2 actualizado con la CSP verificada y el razonamiento de por qué `'wasm-unsafe-eval'` y `blob:` no debilitan el modelo de amenazas de XSS (`connect-src 'self'` sigue bloqueando exfiltración).
6. **Resuelto — bug de dos partes en `orchestrator.ts` (façade, no un motor)**: con #1-#5 resueltos, el Escenario 1 avanzaba hasta abrir el diálogo de export y quedarse colgado indefinidamente en "Preparando exportación…", sin error visible, incluso con timeouts de hasta 180s. Diagnosticado por reproducción directa en consola del browser (bypasseando el input de archivo, que las `mcp__Claude_Browser__*` tools no soportan), con `render.loadDocument` y `export.export` monkey-parcheados para loggear entrada/salida/error y un listener de `unhandledrejection` en `window`. Causa raíz confirmada:
   - **Parte A — buffer detachment**: `RenderEngine.loadDocument(documentId, retained.buffer)` (`orchestrator.ts:744`, y también `:610` en la rama OCR) reutiliza el mismo `ArrayBuffer` (`ImportDocumentInput.buffer`, retenido en `retainedInputs`) que `pdf-engine` ya usó en `getDocument({ data: buffer })` durante el import (`pdf.engine.ts:272-273`). `pdfjs-dist` transfiere/neutraliza el `ArrayBuffer` al entregarlo a su `MessageHandler` interno (comportamiento documentado de pdf.js, aplica incluso sin Worker real de SO) — así que la SEGUNDA llamada a `getDocument({data: buffer})` (la del Render Engine) recibe un buffer de longitud 0. `RenderEngine.loadDocument` lo detecta y rechaza correctamente con `InvalidInputError: "buffer vacío o null en loadDocument."` (`render.engine.ts:150`) — confirmado con traza completa vía el listener de `unhandledrejection`.
     - Para el fixture del Escenario 1 (`text-10p.pdf`, tiene texto real → sin páginas `textless` → **nunca** pasa por `runOcrStage`, la única otra vía que llama `render.loadDocument` antes de export) esto significa que **el primer y único `render.loadDocument` de cualquier documento con texto ocurre recién en export**, y siempre falla. Un documento sin texto (fuerza OCR) fallaría en el mismo punto, pero antes, en `runOcrStage` — mismo bug, otro trigger.
   - **Parte B — el error se pierde en silencio**: la llamada a `this.engines.render.loadDocument(documentId, retained.buffer)` en `runExport` (`orchestrator.ts:744`) **no está envuelta en el `try/catch`** que sí cubre `this.engines.export.export(...)` (líneas 762-770) — ese catch es el único camino a `failPipeline`/`EXPORT_FAILED`. Como `runExport` corre disparado con `void this.enqueueExport(...)` desde `handleExportRequested` (un handler de evento síncrono que no puede propagar rechazos), el `InvalidInputError` se convierte en un **unhandled promise rejection** silencioso: nunca llega a la UI (ni banner de error, ni `EXPORT_FAILED`), el `stage` del pipeline se queda congelado en `"ready"` para siempre, y `ExportProgress.tsx` no tiene ninguna señal para salir de "Preparando exportación…".
   - **Diseño (planificador, 2026-07-22) — `Orchestrator.md` v1.2.1, sin ADR**: no hay cambio de contrato — el fix restaura invariantes que el spec **ya** especificaba (§2 "lo transferido a `PdfPool` es una copia", §12 "no reutilizar buffers transferidos") y que quedaron sin materializar cuando ADR-035 dejó los pools in-process. Parte A: el Orchestrator entrega siempre `slice(0)` del buffer retenido a `PdfEngine.process` y a `RenderEngine.loadDocument` (ambos call sites); el retenido nunca sale del Orchestrator. Parte B: toda la preparación de `runExport` (incluido `loadDocument`; el guard de retained ausente pasa de warn-return a lanzar) entra al `try/catch` → `failPipeline`, y `handleExportRequested` agrega `.catch` terminal sobre `enqueueExport`. Ver §12, §13 casos 23-24 y los 4 tests nuevos de §14.
   - **Implementado y verificado (2026-07-22)**: fix aplicado en `orchestrator.ts` + 4 tests nuevos de `edge.test.ts` (57/57 tests de `anonymization-core/src` verdes). Verificado además con dos reproducciones directas en el Chromium real de Playwright (bypasseando la UI): un documento de 10 páginas hace import + export completo en **~5 segundos**, confirmando que el Core queda genuinamente rápido y correcto — el hang original desapareció por completo.
7. **BLOQUEANTE ACTUAL, no resuelto — bug real en `apps/react-client` (UI, no el Core)**: con el bug #6 resuelto, el Escenario 1 real seguía sin pasar — el diálogo de export avanzaba hasta "Exportando página 10 de 10…" y ahí se quedaba colgado 5+ minutos, sin mostrar nunca el link de "Descargar". Instrumentación directa en consola confirmó que `EXPORT_FINISHED` se emite y `pipeline.store.exportResult` se setea correctamente **en ~5 segundos** (mismo timing que la reproducción sin UI) — el Core y `bus-bridge.ts` funcionan perfecto. El bug está en la UI: la snapshot de accesibilidad tomada al fallar el test **no tiene ningún `dialog "Exportar PDF anonimizado"`** — el diálogo entero desaparece del DOM antes de mostrar el resultado.
   - **Causa raíz confirmada**: `toolbar/ExportButton.tsx` (`ui/Components.md` §2.5) se auto-oculta con `if (!visible) return null` donde `visible = stage === PipelineStage.Ready` (línea 21/36) — y `ExportDialog` se renderiza como HIJO de `ExportButton` (línea 43), controlado por un `useState(open)` LOCAL a ese componente. El Orchestrator transiciona `stage` a `PipelineStage.Done` apenas procesa `EXPORT_FINISHED` (`orchestrator.ts#handleExportFinished`) — casi en el mismo tick síncrono en que `EXPORT_FINISHED` se emite. En ese instante, `visible` pasa a `false` y `ExportButton` (junto con su `ExportDialog` hijo, y todo su estado de progreso/resultado) **se desmonta por completo**, justo antes de que React llegue a pintar la fase "done" con el link de descarga. El banner ("Exportando página 10 de 10…") sigue visible porque lo pinta `toolbar/PipelineStatus.tsx` leyendo `pipeline.store.exportProgress` directamente — un componente totalmente distinto, no afectado por el desmontaje de `ExportButton`, y nada limpia `exportProgress` después de `EXPORT_FINISHED` (`bus-bridge.ts`), así que queda mostrando el último progreso para siempre.
   - **Tensión real entre specs**: `Components.md` §2.5 dice literalmente "Visible: cuando `stage === Ready`", sin ninguna salvedad sobre un diálogo ya abierto. El propio Core (`Orchestrator.md`, `06_Pipeline.md`) transiciona `stage` a `Exporting` → `Done` durante un export — un comportamiento correcto y ya testeado del lado Core. Ningún doc dice qué debe pasar con un componente auto-gated por `stage` cuando el usuario tiene un diálogo hijo abierto en el momento en que `stage` cambia — el mismo patrón de auto-gating lo usa al menos `CancelButton.tsx` (mencionado como "mismo criterio" en el comentario de `ExportButton.tsx`), así que podría no ser el único componente con este riesgo latente.
   - **No apliqué ningún fix** — toca `apps/react-client` (módulo distinto de `orchestrator.ts`, R-1), y la resolución correcta depende de una decisión de diseño entre specs (¿el diálogo debería sobrevivir a un cambio de `stage` mientras está abierto? ¿`ExportButton` debería separar "visibilidad del botón" de "vida del diálogo"? ¿debería el diálogo tener su propio `open` state fuera de `ExportButton`?) que no me corresponde improvisar (regla "ambigüedad: detenerse, no improvisar"). Reportado al humano en la sesión del 2026-07-22.
   - **Diseño resuelto (planificador, 2026-07-22) — fix de UI puro, sin ADR**: el Core se comporta según spec (`Orchestrator.md` §8: `EXPORT_FINISHED` → `Done`); lo que estaba roto era la letra de la UI — `Components.md` §2.5 ("Visible: `stage === Ready`") contradecía §7.2 (el diálogo debe mostrar "Descargar"/"Exportar otro" al finalizar, cosa imposible si ya fue desmontado) y §2.1 no tenía fila para `Done`. Reconciliado: §2.5 pasa a `stage ∈ {Ready, Done}` **y** separa gate de visibilidad (solo el botón) de vida del diálogo (`if (!visible && !open) return null` — un diálogo abierto nunca se desmonta por cambio de `stage`); §2.1 gana `Done` en la fila de `Ready`; regla general nueva §13.9 (cubre el mismo patrón en `CancelButton`/`ConfirmDialog`); `React_Client.md` §2.2: `EXPORT_FINISHED`/`EXPORT_FAILED` limpian `exportProgress: null` (mata el banner stale de `PipelineStatus`). Atajo `Cmd/Ctrl+E` activo solo con el botón visible.
   - **Implementado y verificado (2026-07-22)**: `exportButtonVisibility.ts` nuevo (`isExportTriggerVisible`/`shouldMountExportButton`, puro y testeado — mismo patrón que `pipelineStageLabel.ts`, ya que este repo no tiene tests de render de componentes), `ExportButton.tsx` y `bus-bridge.ts` actualizados, 15 tests nuevos. **Escenario 1 E2E completo pasa en ~7.6s** (antes, colgado 5+ minutos).

**Revisor (2026-07-22, sobre los 5 commits de cierre — `4a9d26a`..`10f26bb`)**: primer veredicto **REJECTED**, dos hallazgos:
1. **Bloqueante, corregido** — `edge.test.ts` (test del seatbelt del bug #6) casteaba `orchestrator as unknown as {...}` para espiar el método privado `enqueueExport`, violando `Code_Standards.md` §10 (el cast de frontera solo se permite contra librerías externas, nunca sobre un tipo propio). Reescrito para ejercitar el seatbelt vía un seam público real (`bus.emit`, la misma instancia que recibe el Orchestrator — sin cast): se fuerza que `failPipeline` lance *dentro de su propio catch* al emitir `PIPELINE_FAILED`, y se verifica con `process.on("unhandledRejection", ...)` que nada escapa. Verificado que el test es significativo: sin el seatbelt en `orchestrator.ts`, falla de verdad. Commit `464c35e`.
2. **No bloqueante, corregido por el planificador** — el nombre del test y el texto del caso 24 de `Orchestrator.md` §14 decían `EXPORT_FAILED`, pero en ese camino (`loadDocument` rechaza antes de invocar `export.export()`) ese evento no puede emitirse — el real es `PIPELINE_FAILED`. El implementador debió reportar la inexactitud del spec en vez de resolverla en silencio nombrando el test con el comportamiento correcto (R-21). Corregido en `Orchestrator.md` v1.3.1 (commit `4433257`).

**Segundo pase del revisor (2026-07-22, rango `3bd83c4..40dc435`, 8 commits): APPROVED.** Confirmó de primera mano (gates corridos por el propio revisor, no asumidos) que ambos hallazgos quedaron resueltos sin regresiones nuevas en los commits 6-8.

**Observaciones no bloqueantes del revisor, sin acción tomada todavía**:
- `tests/e2e/scenario-1-import-edit-export.spec.ts` (comentario cerca de la línea 46): sigue diciendo que `ExportButton` decide su visibilidad por `stage === PipelineStage.Ready` (`Components.md` §2.5) — quedó desactualizado tras el fix del bug #7, que ensanchó el gate a `{Ready, Done}`. No afecta la corrida (el test solo llega a `Ready`), es puramente el texto del comentario.
- `pnpm typecheck` no incluye `tests/e2e/` en su cadena (`tests/e2e/tsconfig.json` no está referenciado desde el `typecheck` raíz) — los `.spec.ts` de E2E solo se type-chequean en runtime al correr Playwright, no en el gate de typecheck. No es un bug, pero vale saberlo si algún día un `.spec.ts` tiene un error de tipos que no se detecta hasta la corrida real.
- La edición de `docs/architecture/08_Security_Model.md` §3.2 (documento de arquitectura — por `ai/AI_Development_Guide.md` §7, autoría del planificador) se incluyó dentro del commit de implementación de ADR-039 (`4a9d26a`) en vez de ir en un commit `docs:` aparte. La resolución de la CSP en sí estaba autorizada (ya documentada por el planificador en `Hito10_Observaciones_Revision.md` antes del commit), pero el agrupamiento de commits no siguió el patrón habitual de este repo (docs primero, en su propio commit). Sin acción — cosmético, no afecta la validez del contenido.

**Estado dejado**: PR10 cerrado. Los 5 commits de implementación (`4a9d26a` ADR-039, `5c921db` bug #6, `0e7af1e` bug #7, `0b581d0` ADR-040, `10f26bb` tests/e2e/) + el fix del hallazgo del revisor (`464c35e`) + la corrección del spec (`4433257`) están en `feat/hito10-react-client`. Gates completos verdes: `pnpm lint`, `pnpm typecheck`, `pnpm test` (759 tests), `pnpm test:contract` (187 tests), y la suite E2E completa (Escenario 1 pasa en ~7.6-7.7s, Escenario 6 pasa, Escenario 8 correctamente skipeado por la ambigüedad ya documentada). Issues de GitHub #3 y #6 pendientes de cerrar manualmente (`gh` no está autenticado en este entorno).

---

## PR11 — Transporte de workers (`CoreRuntimeOptions`, ADR-036 §2-§4; cerrado)

Alcance exacto de la fila de PR11 en ADR-036 §8: `WorkerLike`/`WorkerFactory`/`WorkerEntryKind`/`CoreRuntimeOptions` en `shared` (Contracts.md §3.5), `createCore(config?, runtime?)`, modo de despacho por `postMessage` en `worker-pool.ts` (con fallback in-process bit-idéntico al Hito 9/ADR-035 cuando no hay factory), variante `EVENT` de `WorkerOutbound` (ADR-036 §3), y fakes estructurales de `WorkerLike` en los tests (sin `Worker` real de browser). Ningún motor tocado, nada en `apps/react-client` todavía (eso es de los PR12-16).

**Revisor, primer pase — REJECTED**: un solo motivo bloqueante. `handleWorkerTransportError` (`worker-pool.ts`) citaba `05_Worker_Architecture.md` §9 ("crash de worker → marcar dead, reemplazar, **reintentar el job si retryable**") pero rechazaba con `Error` genérico — nunca podía reintentar (`isRetryable` exige `instanceof EngineError`) y violaba `Code_Standards.md` §7. Raíz real: **no existe ningún `EngineErrorCode` para "crash de transporte"** (confirmado contra `shared/src/enums.ts` — todos son por-motor o 4 genéricos que no encajan); inventar uno nuevo requiere el proceso de docs (I-4). El revisor marcó esto como ambigüedad no reportada por el implementador.

**Decisión del humano**: opción (b) del revisor — diferir el manejo pleno de crash-con-reintento a los PR12-16 (recién ahí hay un worker real con re-priming de `INIT`/`load-document` contra el cual un reintento tiene sentido de verdad), en vez de inventar el error code o implementar el retry ahora sin poder validarlo. Fix aplicado: el rechazo pasa a `InvalidInputError` (mismo criterio que `toSerializedError` ya usa en el mismo archivo, sin inventar código, citando I-4); `retryable: false` de fábrica documenta que no reintenta, a propósito; el docstring deja la ambigüedad del error code explícitamente registrada, no resuelta en silencio.

**Revisor, segundo pase — APPROVED.** Verificó de primera mano (gates propios) que el fix resuelve el hallazgo sin regresiones. Observaciones no bloqueantes:
- No se pudo aislar el diff del fix del segundo pase vía `git diff` porque el PR11 completo se commiteó como una sola unidad recién al final (sin baseline intermedio commiteado) — el revisor confirmó la ausencia de regresión por inspección directa en vez de un diff antes/después estricto. Sin acción — si se quiere una garantía git más estricta en PRs futuros con hallazgos del revisor, conviene commitear cada pase (implementación inicial + fix) por separado antes de pasar al siguiente PR.
- Deuda registrada, no cerrada: el manejo pleno de crash-con-reintento (`05_Worker_Architecture.md:270`) queda diferido a los PR12-16. Cuando aparezca el primer worker real, la clasificación `INVALID_INPUT` + no-retry debe revisitarse; si se introduce un `EngineErrorCode` dedicado a transporte, primero va `Contracts.md`/ADR y después código (R-19). El planificador debería tener esto en el radar del hito para que no se pierda.
- Pre-existente, fuera del alcance del fix (ya aprobado sin objeción en el primer pase): quedan dos `throw new Error(...)` en `worker-pool.ts` como guardas de invariante interno (`assignRemoteSlot`/`workerForSlot`, documentados como "no debería pasar") y uno como wrapper defensivo de un valor no-`Error`. No son el path de fallo público de dominio que exige `Code_Standards.md` §7 (que aplica a funciones públicas), así que no son una objeción — solo trazabilidad.

**Estado dejado**: PR11 cerrado, commit `c75e710` en `feat/hito10-react-client`. Gates verdes: `pnpm lint`, `pnpm typecheck`, `pnpm test` (781 tests), `pnpm test:contract` (194 tests). Sin E2E propio (PR11 no tiene consumidor real todavía — ningún worker real corre hasta PR12).

---

## PR12 — Workers reales: PdfWorker (cerrado, ADR-041 + ADR-042)

**2026-07-22 — el implementador se detuvo al arrancar, sin tocar archivos** (regla "ambigüedad: detenerse, no improvisar", `AI_Development_Guide.md` §5): `PdfEngine.fuseOcrPage` resolvía el documento en el `Map<string, Document>` interno de la instancia, pero con workers reales la instancia vive dentro del worker y `worker-pool.ts#assignRemoteSlot()` no tiene afinidad por `documentId` — un `fuseOcrPage` podía caer en un worker que nunca parseó ese documento. Sin transporte previsto: `WorkerJobType`/`WorkerInbound` son unions cerrados (ADR-036 §4) y el precedente broadcast de `load-document` no transfiere a una mutación. El implementador levantó 4 opciones sin decidir (jobType nuevo / sticky routing / pool size 1 / función pura).

**Verificación del planificador antes del veredicto**: el informe era fiel al código (`pdf.engine.ts` con el `Map`; `orchestrator.ts` reteniendo su propia copia canónica en `this.documents` y persistiendo ya el resultado de cada fusión). La verificación destapó dos argumentos adicionales que el informe no vio: (a) si la fusión es pura no necesita worker en absoluto — corre host-side en el Orchestrator, que como façade puede importar de `pdf-engine` (P-1); (b) las opciones con round-trip de worker introducían una carrera lost-update entre `OCR_PAGE_FINISHED` cercanos que hoy no existe; y un tercer problema estructural: aun con afinidad, `releaseDocument` habría requerido el mismo ruteo (o dejaba un leak de `Document`s retenidos por worker).

**Decisión del humano — opción 4, ADR-041** (`adr/ADR-041-FuseOcrPage-Funcion-Pura-Sin-Estado-Retenido.md`): `fuseOcrPage` pasa a función pura síncrona `(document, pageIndex, words) → Document` exportada por `pdf-engine` y ejecutada host-side; `PdfEngine` queda sin estado por documento (`releaseDocument` eliminado, ADR-020 §7/ADR-021 §7-Pdf superseded); el transporte no se toca (ratifica ADR-036 §4). El ADR audita además el estado retenido de los demás motores: `render-engine` queda marcado **bloqueante pre-PR13** (estado por documento más allá del broadcast `load-document`: `cache`, `groupOverrides`, `lastInputs`, `pendingRenders`); `ocr`/`export` verificados stateless; `ner` solo estado global por proceso (modelo). Sin fixture E2E nuevo en PR12 (la fusión ya no cruza la frontera del worker); el fixture de PDF escaneado real queda para PR14 (OcrWorker).

**Docs actualizados en la misma pasada** (commit `docs:` del planificador, previo a reanudar la implementación — R-2/R-19/R-21): `PDF_Engine.md` v1.3.0, `Orchestrator.md` v1.4.0, `OCR_Engine.md` v1.1.1, `04_Event_System.md` (filas `OCR_PAGE_FINISHED`/`DOCUMENT_CLOSED`), notas de supersede en ADR-020/ADR-021, `MVP.md` §4.

**Estado dejado**: PR12 desbloqueado con su alcance original (entry-point + host-bridge + subpath `"./worker"` + wiring en la app) más el item 20 de `PDF_Engine.md` §15 (extracción de la función pura y adaptación de tests). Implementación pendiente.

**Segunda ambigüedad (2026-07-22, levantada por el implementador al terminar el entry-point; confirmada por el revisor en su primer pase como bloqueante y transversal a los 5 PRs de worker)**: `WorkerOutbound.COMPLETED.result` estaba tipado `Serializable`, pero los `*EngineOutput` son `interface`s y TypeScript no asigna una `interface` nombrada a un index signature (microsoft/TypeScript#15300) — el entry-point del PdfWorker quedó con un `@ts-expect-error` documentado como pendiente de decisión (manejo correcto: sin cast inventado, `Code_Standards.md` §2/§10). Verificación del planificador: `Serializable` se usa solo en tres campos de `WorkerOutbound` (nada más se afloja), y la regla "el tipo concreto se afina al cruzar la frontera" ya estaba escrita dos veces (`RUN.payload`/`INIT.config` `unknown` por ADR-019 en §2.1; `EVENT.payload` `unknown` por ADR-036 §3 — segundo precedente que el informe no había citado). **Decisión del humano — opción C acotada, ADR-042** (`adr/ADR-042-WorkerOutbound-Completed-Result-Unknown.md`): `COMPLETED.result` pasa a `unknown` (el host-bridge lo afina a su `*EngineOutput` con comentario de frontera estilo ADR-019); `PROGRESS.partial` y `LOG.meta` conservan `Serializable` (transportan literales ad-hoc, donde el chequeo sí opera). Patrón sancionado para PR13–16, sin nueva excepción de `as unknown as` (ADR-031 §4 sigue única). Docs: `05_Worker_Architecture.md` §2.2. Aparte de este punto, el revisor aprobó funcionalmente el resto del PR (función pura de ADR-041, entry-point, wiring; los 4 errores de lint del primer pase ya corregidos en verde) — con el fix de este ADR (sacar el `@ts-expect-error` y afinar en el host-bridge), PR12 va a segundo pase.

**Bug real adicional, encontrado y corregido al validar (no en el alcance original de ningún ADR de este PR)**: al re-correr el Escenario 1 E2E contra un dev server con cache de Vite recién limpiada (`node_modules/.vite` borrado), el pipeline fallaba con "El archivo no es un PDF válido" sobre un PDF genuinamente válido — la corrida verde anterior había sido un falso positivo contra un bundle stale que nunca ejerció el transporte remoto de verdad. Causa: cada `Worker` real tiene su propio scope global; `apps/react-client/src/main.tsx` configura `GlobalWorkerOptions.workerSrc` de `pdfjs-dist` solo para el hilo principal (bug histórico del PR10, ya resuelto ahí) — el `PdfWorker` (`entry.ts`) importa su propia instancia de `pdfjs-dist` en su propio scope y nunca lo configuraba, mismo síntoma, contexto nuevo. Fix: `entry.ts` configura su propio `workerSrc` con un import `?url` propio (mismo patrón que `main.tsx`), con un ambient `.d.ts` nuevo (`worker/vite-worker-env.d.ts`, solo tipos, sin agregar `vite` como dependencia runtime de `pdf-engine`). El revisor confirmó en su segundo pase que esto no era una ambigüedad nueva a reportar (ADR-036 §2 ya designaba el entry-point como "bundleado por Vite" y a la app como resolutor de `?worker`; el fix aplica decisiones ya tomadas a un bug de runtime genuino) y lo verificó de primera mano corriendo el E2E contra cache limpia él mismo (38.8s totales, ~32s de cold-start real).

**Segundo pase del revisor: APPROVED.** Gates verdes de primera mano (`pnpm lint`, `pnpm typecheck`, `pnpm test` 790, `pnpm test:contract` 192, `pnpm test:coverage` sobre los pisos) + Escenario 1 E2E verde contra cache de Vite limpia. Sin regresión en la parte de ADR-041 (aprobada en el primer pase).

**Estado dejado**: PR12 cerrado, commit `ba28358` en `feat/hito10-react-client`. `render-engine` queda con la auditoría bloqueante de ADR-041 §5 pendiente de decisión propia antes de arrancar PR13 (estado retenido más allá del broadcast `load-document`: `cache`, `groupOverrides`, `lastAnonymizedInputs`/`lastOriginalInputs`, `pendingRenders`).

---

## PR13 — RenderEngine reparto host/worker (ADR-043) + fix real: visor en blanco para documentos con texto nativo (cerrado, revisor: APPROVED)

Alcance: migración de `render-engine` al reparto host/worker de ADR-043 (la clase `RenderEngine` completa queda host-side; el worker corre un kernel sin estado por documento salvo los `PDFDocumentProxy`; todas las vías de render convergen en `RenderPool.dispatch`/`broadcast`; `unload-document` nuevo; re-priming de workers nuevos/reemplazados) — `Render_Engine.md` §15 items 22-24. En la misma sesión, fix real (sin ADR) de un bug de PR10 que había pasado desapercibido: para un documento con texto nativo (sin páginas OCR), `RenderEngine.loadDocument` nunca se invocaba hasta el export — la UI emite `RENDER_REQUESTED` en cuanto el pipeline llega a `Ready`, mucho antes, y `RenderEngine` lo descartaba en silencio (visor en blanco indefinido). Restaura la invariante ya escrita en `Orchestrator.md` §2 (v1.4.1, §13 caso 25, §14).

**Observaciones no bloqueantes del revisor, sin acción tomada todavía**:
- **Dead code intencional en `worker-pool.ts`**: la clave `"render"` en `PoolKey` (línea 52), `JOB_TYPE_BY_POOL` (línea 685) y `poolSizeFor` (`orchestrator.ts` líneas 1183-1184) quedó sin call site vivo — el `RenderPool` ahora lo crea `create-core.ts` directo. El implementador la dejó a propósito para no ampliar el diff de `WorkerPoolManager` más allá de lo que ADR-043 pide. Candidato a limpieza en un follow-up.
- **Nombre del test de caso 24** (`Orchestrator.md` §14): `"loadDocument failure during export emits PIPELINE_FAILED, no hang"` ahora dispara vía `getSnapshot` (ya no puede fallar en `loadDocument` durante el export de un documento recién importado, porque el fix lo carga antes). El mecanismo que prueba sigue siendo el correcto (§13.24), pero el nombre quedó más específico que su disparador real — candidato a relabelar en un futuro PR de docs.
- **E2E Escenario 1 no re-corrido por el revisor**: el implementador lo corrió (~42s, pass) contra el wiring real de `RenderWorker`; el revisor se apoyó en ese resultado + los 4 gates obligatorios (correr Playwright queda fuera del subset mínimo pre-PR). Si se quiere cierre de primera mano del checklist item 24, correr `pnpm test:e2e` del Escenario 1 antes del merge.
- **Gap conocido del worker real**: en modo worker, el warning de "OffscreenCanvas no disponible" no tiene puente de bus/logger (solo cableado en el fallback in-process, mismo precedente que `PdfWorker`); el render igual falla con `RenderPageFailedError`, sin pérdida de robustez, solo de visibilidad del log.
- **Redundancia benigna en `reprimeWorkers`**: el broadcast de re-priming reenvía `load-document` a **todos** los workers vivos, no solo al nuevo — recarga determinística (ADR-030), documentada como redundante pero inofensiva.

**Estado dejado**: PR13 cerrado, revisor: **APPROVED** (checks de primera mano: kernel sin estado, despacho unificado, wire shape/orden de discriminación, re-priming con orden garantizado, interfaz/eventos/supersede/cache sin cambios, gates y prohibiciones). Gates verdes: `pnpm lint`, `pnpm typecheck`, `pnpm test` (807 tests), `pnpm test:contract` (192 tests); cobertura `render-engine` 92-95%. Working tree sin commitear en `feat/hito10-react-client`, a la espera de autorización explícita del humano para `git commit`.

---

## Post-PR13 — tres bugs reales del visor encontrados en prueba manual (ADR-044 + 2 fixes de UI; cerrado, revisor: APPROVED)

Al probar manualmente el visor tras cerrar PR13, el humano reportó tres síntomas. Investigados a fondo antes de asignar nada al implementador (regla de ambigüedad: no improvisar diseño):

1. **Bug real, sin ADR**: el visor quedaba en blanco hasta que el usuario scrolleaba. `PdfViewer` pide el primer render apenas conoce `pageCount` (`DOCUMENT_PARSED`), mucho antes de `Ready` — y ese primer `RENDER_REQUESTED` casi siempre perdía la carrera contra el `await pool.broadcast(...)` real de `RenderEngine.loadDocument` (PR13), descartado en silencio sin cola ni reintento (comportamiento documentado de `Render_Engine.md` §8, no un bug de ese motor). Fix de UI puro (sin tocar ningún motor): `PdfViewer.tsx` reintenta el render de las páginas montadas una sola vez por documento al observar `stage === Ready` — invariante que ya garantiza v1.4.1 (`loadDocument` siempre resuelve antes de `Ready`). Nuevo `readyRenderTrigger.ts` (predicado puro) + test.
2. **Bug real, arquitectónico — ADR-044** (`adr/ADR-044-Preview-Grupos-Mediacion-Orchestrator.md`): el panel anonimizado nunca difería del original. Diagnóstico: `RENDER_REQUESTED` no transporta `replacements` por diseño (reconstruye desde el último input recordado, o `[]` si la página nunca se renderizó); `RenderEngine` solo escuchaba `GROUP_REPLACEMENT_CHANGED`/`GROUP_TOGGLED` (eventos de cambio, no de creación) y su `pageGroupIndex` solo se poblaba si ya había reemplazos no vacíos — deadlock de arranque. Peor: el mecanismo de overrides era lossy (un toggle off→on no podía resucitar reemplazos ya filtrados). El humano decidió, entre tres opciones de diseño, que el **Orchestrator media**: se suscribe a `ENTITY_GROUP_CREATED`/`UPDATED`/`REMOVED`, computa reemplazos autoritativos vía `grouping.getSnapshot` + `buildPageReplacements` (compartida desde `export-engine`), e invoca `RenderEngine.renderPage` directo (patrón ADR-014) — seed en `GROUPING_FINISHED`, flush coalescido por microtask para ediciones posteriores. El RenderEngine **retira** `requestDeltaRender`, `groupOverrides`, `pageGroupIndex` y las suscripciones a Grouping (sin callers externos verificado); pasa a escuchar solo `EventChannel.UI`. `Contracts.md` no cambia. Docs: `Orchestrator.md` v1.5.0, `Render_Engine.md` v1.5.0, `Export_Engine.md` v1.1.1, `04_Event_System.md` §6/§11.
3. **No confirmado, sin fix aplicado**: un salto grande de scroll aparentaba volver a la página 1. Hipótesis (alta confianza, no probada): entradas viejas del `Set` de `IntersectionObserver` en `PageVirtualizer.tsx` podrían quedar sin limpiar al momento en que corre el `requestAnimationFrame` coalescedor. El implementador intentó reproducirlo en Chromium real vía Playwright (salto instantáneo de `scrollTop`, ráfagas de `wheel`, scroll inercial sampleado) y **no logró disparar el síntoma** — decisión correcta de no aplicar un fix sin un test que lo justifique. Quedó `tests/e2e/viewer-scroll-jump.spec.ts` como guardia de regresión (el revisor confirmó que el test es genuino y representativo, no vacío) pero `PageVirtualizer.tsx` sin tocar. **Pendiente de reproducir en un browser real (no headless) si el síntoma persiste** — candidato a que el humano lo intente de nuevo con pasos exactos (tamaño de PDF, velocidad de scroll) si vuelve a aparecer.

**Hallazgo del revisor, corregido en la misma sesión (v1.5.1, sin ADR)**: el seed/flush de ADR-044 armaba su `ctx` con la señal cancelable del documento (`abortRegistry`); en la cancelación de un `reanalyze` (ADR-038 §6: `abort()` antes de `await finishSession`), el seed que dispara el `GROUPING_FINISHED` síncrono corría con esa señal ya abortada, rechazando antes de `rememberInput` — no reabría el bug 1 (el documento ya tenía el preview poblado de una corrida anterior) pero contradecía lo que el propio ADR-044 §3 prometía, y el test que lo cubría pasaba de forma vacía (el mock no chequeaba `abortSignal`). Fix: `seedAnonymizedPreview`/`flushDirtyPages` arman su `ctx` con una señal propia (`mediatedPreviewCtx`), nunca ligada a `abortRegistry` — `OCR`/`NER`/export siguen cancelables sin cambios. Verificado por el implementador y luego por el revisor revirtiendo el fix y confirmando que el test lo detecta.

**Observaciones no bloqueantes, sin acción tomada**:
- El test de caso 2 de `Render_Engine.md` (grupo deshabilitado → texto original), que ejercitaba el mecanismo retirado (`GROUP_TOGGLED`), quedó reformulado sobre la vía directa — el revisor confirmó que sigue probando genuinamente el caso, dividido entre `render-engine` (un solo `fillText`) y `export-engine` (`buildPageReplacements` real filtra el grupo deshabilitado).
- Empaquetado: el working tree agrupa los docs del planificador (ADR-044 + specs actualizados) con la implementación de los 3 bugs en una sola sesión — al commitear, separar en commits por bug + un commit de docs previo, mismo patrón que PR13.
- Renders del preview mediado (seed/flush) dejan de ser cancelables por el `AbortController` del documento tras el fix v1.5.1 (trade-off deliberado: son best-effort e inmunes al supersede desde ADR-044 §3; ahora también a la cancelación). Costo marginal de eficiencia si el documento se cierra a mitad de un render mediado (el render completa en vano, pero el resultado se descarta sin problema), no de correctitud.

**Estado dejado**: cerrado, revisor: **APPROVED** (dos pases: el primero sobre el batch completo, el segundo puntual sobre el fix v1.5.1). Gates verdes: `pnpm lint`, `pnpm typecheck`, `pnpm test` (815 tests), `pnpm test:contract` (195 tests); cobertura `render-engine`/`export-engine`/façade sobre los pisos. E2E: Escenario 1 (~44s) y el nuevo `viewer-scroll-jump.spec.ts` (~39s), ambos verdes. Working tree sin commitear en `feat/hito10-react-client`, a la espera de autorización explícita del humano para `git commit`.

---

## Bug post-PR13 — los dos paneles del visor muestran lo mismo (diseño resuelto: ADR-044; implementación pendiente)

Detección: prueba manual sobre el Escenario 1 tras cerrar PR13. Diagnóstico del implementador, confirmado por el planificador leyendo el código: `RENDER_REQUESTED` no trae `replacements` por diseño (el motor los reconstruye del último input recordado, o `[]` si la página nunca se renderizó), y **nadie** alimenta nunca el primer input `anonymized` con reemplazos reales — Render solo escuchaba eventos de *cambio* (`GROUP_REPLACEMENT_CHANGED`/`GROUP_TOGGLED`), no de creación, y el único cómputo de reemplazos desde grupos vivía en el export (`buildPageReplacements`). Deadlock doble: `pageGroupIndex` solo se puebla con `replacements.length > 0`, así que el delta render tampoco encontraba nunca páginas afectadas. En el análisis se destapó un segundo bug del mismo cableado: el delta render por overrides es lossy — un toggle off→on no puede resucitar los `Replacement` filtrados (el input recordado se sobreescribe con la lista ya filtrada) — y una carrera latente: `GROUP_TOGGLED`/`GROUP_REPLACEMENT_CHANGED` co-emiten siempre un `ENTITY_GROUP_UPDATED`, así que cualquier mediación parcial habría duplicado renders por edición sin orden de finalización garantizado.

**Diseño resuelto 2026-07-23 — ADR-044** (`adr/ADR-044-Preview-Grupos-Mediacion-Orchestrator.md`, opción "Orchestrator media" del prompt del implementador, ajustada por el planificador): el Orchestrator se suscribe a `ENTITY_GROUP_CREATED`/`UPDATED`/`REMOVED`, computa reemplazos autoritativos con `grouping.getSnapshot` + `buildPageReplacements` (que `export-engine` pasa a exportar) e invoca `renderPage({ kind: "anonymized", mode: "preview" })` directo (patrón ADR-014, call site preexistente por ADR-043); seed en `GROUPING_FINISHED` + flush coalescido por microtask post-`Ready`. El RenderEngine **deja de escuchar el canal `grouping`** y se retiran `requestDeltaRender`/`groupOverrides`/`pageGroupIndex` (mueren los dos bugs y la carrera con ellos). Un solo PR toca tres costuras sancionadas (façade/Orchestrator + retiros en `render-engine` + una línea de export en `export-engine`; precedente PR13/ADR-043). Docs: `Orchestrator.md` v1.5.0, `Render_Engine.md` v1.5.0, `Export_Engine.md` v1.1.1, `04_Event_System.md` §6/§11, `02_System_Diagrams.md` §4, `06_Pipeline.md` §10/§11.

---

## PR14 — OcrWorker (fork bloqueante resuelto: ADR-045; cerrado, revisor: APPROVED)

El implementador cerró la parte no ambigua con gates verdes (827 tests, cobertura ≥ 98% en `ocr-engine`) y se detuvo en el fork de cómo hacer real el despacho a OcrWorker. Su hallazgo, verificado por el planificador: el plan original (Orchestrator despacha `ocr-page` por página, el pool hereda el retry) dejaba **cuatro problemas** — (1) carrera EVENT/COMPLETED: el motor corriendo en el worker deposita las `Word[]` en una cache **local al worker** (gap documentado en la cabecera del `entry.ts` actual) y emite `OCR_PAGE_FINISHED` por el puente, sin orden garantizado respecto del `COMPLETED` que trae los datos — el Orchestrator podía fusionar una página vacía, contradiciendo ADR-014 §1 ("las `Word[]` las deposita el **lado host**"); (2) retry duplicado motor×pool (hasta `(maxRetries+1)²` reconocimientos); (3) eventos del mismo motor emitidos desde los dos lados de la frontera; (4) resolverlo dentro del plan original exigía retirar `processPages` y mover `OCR_STARTED`/`OCR_FINISHED` al Orchestrator (cambio de contrato nivel ADR-041).

**Decisión del humano — opción A, ADR-045** (`adr/ADR-045-OcrEngine-Pool-Propia-Kernel-Puro.md`, espejo de ADR-043/render): la clase `OcrEngine` queda entera host-side (loop por página, retry/timeout, eventos, depósito en cache — la secuencia `kernel → ctx.cache.set → OCR_PAGE_FINISHED` es una sola ruta host, la carrera muere por construcción); el worker corre un **kernel de reconocimiento sin estado por documento** (§7.2 ya lo describía así); `new OcrEngine(pool?)` + despacho con `maxRetriesOverride: 0` (retry único: el del motor; timeouts normalizados a `OcrTimeoutError`). Requisito explícito del humano preservado por construcción: **flujo incremental por página** (`OCR_PAGE_FINISHED` a medida que cada página termina, fusión inmediata, progreso en UI). Costo asumido: reescribir `worker/entry.ts` (construido como envoltorio del motor) como kernel puro. Docs: `OCR_Engine.md` v1.2.0 (§2/§6/§12/§14/§15 items 19–21), `05_Worker_Architecture.md` §7.2 + nota de excepciones de §1.

**Implementado (2026-07-24)**: `OcrJobPool`/`IMMEDIATE_POOL`/constructor `pool?` espejo campo-por-campo de `RenderJobPool` (`render.engine.ts`), sin `broadcast` (OCR no tiene ciclo load/unload de documento — ADR-041 §5); `worker/kernel.ts` nuevo (setup first-party de tesseract, recreate-on-language-change, timeout/abort racing, extracción de palabras/confidence); `worker/entry.ts` reescrito como kernel puro (sin bus/cache puente); `normalizeTimeout()` en el borde del puerto; `create-core.ts` construye `ocrPool` e inyecta `new OcrEngine(ocrPool)`; `orchestrator.ts#runOcrStage` deja de envolver `processPages` en `pool.dispatch()` (mismo tratamiento que ADR-043 dio a `rasterizePage`). 6 tests nuevos de `OCR_Engine.md` §14.

**Revisor: APPROVED** (gates corridos de primera mano). Verificó punto por punto: espejo con `render-engine` correcto (con la diferencia deliberada de no tener `broadcast`); `maxRetriesOverride: 0` confirmado como único punto de dispatch; orden `ctx.cache.set` → `OCR_PAGE_FINISHED` host-side verificado en código (mata la carrera EVENT/COMPLETED por construcción, restaura ADR-014 §1); normalización de timeout confirmada en `ocr.engine.ts` antes del chequeo de reintento; recreación de instancia tesseract por cambio de idiomas verificada en el kernel (cubre `reanalyze` de ADR-038 §5.3); los 6 tests de §14 presentes con nombres que reflejan su intención; checklist §15 items 19-21 completos (la parte E2E con PDF escaneado real queda diferida a Hito 11, admisible por precedente ADR-041). Diff scope, spec sync, contratos y prohibiciones (P-1..P-10) sin objeciones.

**Observaciones no bloqueantes del revisor, sin acción tomada**:
- `poolWorkerFactories.ocr` en `orchestrator.ts` queda como dead code (ya no se llama `getPool("ocr")`) — mismo tratamiento exacto que `render` recibió tras ADR-043/PR13 (tampoco se limpió). Inofensivo: `WorkerPoolManager` crea pools perezosamente, nunca se instancia un pool duplicado. Candidato a limpieza conjunta (`ocr` + `render`) en un futuro PR, fuera del alcance de este.
- La transferencia zero-copy de `imageData` la resuelve `WorkerPool` en modo remoto (fuera de `ocr-engine`); si el pool no la agregara a la transfer list sería a lo sumo una degradación de perf, no un bug de correctitud — no verificado en este PR, mismo criterio que los motores anteriores.

**Estado dejado**: PR14 cerrado. Gates verdes de primera mano: `pnpm lint`, `pnpm typecheck`, `pnpm test` (828 tests/69 archivos), `pnpm test:contract` (198 tests/10 archivos); cobertura `ocr-engine` ~99% líneas (`ocr.engine.ts`), 93-97% en los módulos nuevos de `worker/`. Working tree sin commitear en `feat/hito10-react-client`, a la espera de autorización explícita del humano para `git commit`.

---

## PR15 — NerWorker (fork bloqueante resuelto: ADR-046; cerrado, revisor: APPROVED)

El implementador exploró PR15 y se detuvo con un informe de ambigüedad **antes de escribir una línea** (`ai/AI_Development_Guide.md` §5): `NER_Engine.md` v1.1.0 (§7, §15 items 5–6) y `05_Worker_Architecture.md` §7.3 seguían describiendo el patrón previo a ADR-043/045 —el motor crea su propio `NerPool` y emite `ENTITY_FOUND` por ocurrencia **desde dentro del worker**—, mientras `render-engine` y `ocr-engine` ya habían migrado al reparto host-side + kernel. Señaló además que `ADR-041` §5 marca a `ner-engine` como "Libre", pero esa auditoría no contempla el problema que bloqueó PR13/PR14. **Diagnóstico confirmado por el planificador: era el mismo fork, intacto.**

**Decisión — ADR-046** (`adr/ADR-046-NerEngine-Pool-Propia-Kernel-Puro.md`, tercer espejo de ADR-043/045; el patrón de fondo ya lo había decidido el humano, este ADR lo aplica): la clase `NerEngine` queda entera host-side (loop por página, partición en batches, retry/timeout, mapeo span→`Occurrence` con bbox, los seis eventos); el worker corre un **kernel de inferencia sin estado por documento** (`NerPagePayload` de **un batch** → `COMPLETED { spans }`); `new NerEngine(pool?)` + despacho con `maxRetriesOverride: 0` y `priority: 80`; normalización **por `code`** de `NER_TIMEOUT` y `NER_MODEL_MISSING` en el borde del puerto (dos códigos, no uno como OCR: el motor bifurca por ambas clases y `EngineError.deserialize` devuelve instancias genéricas).

Cinco problemas verificados contra el código, resueltos: (1) doble canal para el mismo dato — `ENTITY_FOUND` por el puente y las mismas ocurrencias por `COMPLETED`, sin orden garantizado, con `grouping-engine` alimentándose **solo** del stream de eventos; (2) retry duplicado motor×pool; (3) los seis eventos repartidos entre los dos lados de la frontera; (4) despachar `processPages` entero contradice que `timeouts/maxRetries["ner-page"]` estén especificados **por página**; (5) **problema propio de NER, sin equivalente en OCR**: `NER_MODEL_LOADING`/`NER_MODEL_READY` son eventos observables que solo pueden originarse dentro del worker. Solución del (5): el ciclo de vida del modelo cruza por el canal **`PROGRESS`** del transporte (que ya existe en `WorkerOutbound`, con `NerKernelProgress` en `partial`) y el motor lo traduce en host — `NER_MODEL_READY` deduplicado a uno por instancia. Eso obliga a la única costura ajena nueva: `DispatchParams.onProgress` en `worker-pool.ts` (hoy el pool descarta los `PROGRESS`), sancionada en el ADR con el precedente de `broadcast` (ADR-043 §4). De paso, ADR-039 (`wasmPaths`) obtiene por fin transporte real al worker: viaja en el payload del job, sin depender del gap de `INIT`.

Docs actualizados: `NER_Engine.md` v1.2.0 (nota de cabecera, §2, §6 con `constructor(pool?)` + semántica del despacho, §7 dónde se emite cada evento, §11 origen/cruce de errores, §12, §13 casos 16–18, §14 con 10 tests nuevos, §15 items 20–24), `05_Worker_Architecture.md` (§1 tercera excepción, §2.2 enrutamiento de `PROGRESS`, §7.3 reescrito como kernel), `03_Data_Model.md` §18 (`NerPagePayload` por batch + `quantization`/`wasmPaths?`, `NerKernelSpan`, `NerKernelProgress`), `ADR-041` §5 (matiz del alcance de la auditoría).

**Implementado (2026-07-24)**: `ner.engine.ts` reescrito completo como clase host-side (`NerJobPool`/`IMMEDIATE_POOL`, constructor `pool?`); `worker/kernel.ts` nuevo (inferencia sin estado por documento, carga perezosa del clasificador con 2 intentos, agregación de tokens BIO en spans, `configureTransformersEnv`); `worker/entry.ts` nuevo (mensajería `INIT`/`RUN`/`CANCEL`/`DISPOSE`, reenvía `PROGRESS` sin traducir); normalización por `code` de `NER_TIMEOUT`/`NER_MODEL_MISSING` en el borde del puerto; traducción `PROGRESS` → `NER_MODEL_LOADING`/`NER_MODEL_READY` deduplicada por instancia (`modelWarm`). Costuras ajenas sancionadas por ADR-046 §4/§7: `worker-pool.ts` gana `DispatchParams.onProgress` (enrutamiento de `PROGRESS` por `jobId` en `handleWorkerMessage`); `create-core.ts` construye e inyecta `nerPool`; `shared/src/types.ts` gana `NerKernelSpan`/`NerKernelProgress` y los dos campos nuevos de `NerPagePayload`; wiring del `NerWorker` real en `apps/react-client/src/core-adapter/index.ts`. 10 tests nuevos de `NER_Engine.md` §14 (2 de contract, 2 de unit, 6 entre kernel/entry-point).

**Ambigüedad menor detectada y resuelta por el implementador durante la implementación (no bloqueante, reportada igual)**: ADR-046 §7 cita textualmente "sus dos call sites (`runDetectionStage` y `runReanalyzeNerOnFlow`)" dejando de envolver `processPages` en `pool.dispatch()`. El código real tiene un **tercer** call site idéntico dentro de `runReanalyzeOcrFlow` (`orchestrator.ts:530`), no mencionado por el ADR — un subconteo fáctico, no una decisión de arquitectura distinta. El implementador aplicó la misma transformación mecánica a los tres (omitirla habría reintroducido el retry duplicado, problema 2 del Contexto de ADR-046, en el flujo de reanalyze por OCR). El revisor confirmó independientemente que era lo correcto. **Conviene una errata de una línea en ADR-046 §7** (dos → tres call sites, agregando `runReanalyzeOcrFlow`) para que el doc no describa un estado que el código ya superó — sin acción tomada todavía.

Dos fixes de infraestructura de test, también reportados y verificados por el revisor sin objeción: el glob de `test.include` en `vitest.config.ts` se amplió de `packages/**/src/__tests__/**/*.test.ts` a `packages/**/src/**/__tests__/**/*.test.ts` (el viejo no matcheaba la ruta anidada `src/worker/__tests__/` que `NER_Engine.md` §14 pide — esos tests nunca se hubieran descubierto bajo `pnpm test`); y el alias `@anonly/ner-engine` de `vitest.config.ts` (preexistente, para `tests/integration/`) se ancló con `RegExp` (`/^@anonly\/ner-engine$/`) porque el nuevo subpath `"./worker"` de `ner-engine/package.json` exponía un bug de resolución por prefijo de Vite (el alias como string plano capturaba también `@anonly/ner-engine/worker` y lo resolvía mal).

**Revisor, primer pase — REJECTED. Un solo bloqueante**: faltaba el test unitario de la costura nueva `PROGRESS` → `onProgress` en `worker-pool.ts`, pedido textualmente por ADR-046 (Validación, viñeta 5: "un `PROGRESS` con `jobId` de un job pendiente llega a su `onProgress`; uno de un job ya resuelto se descarta sin lanzar"). Ningún test del repo ejercitaba la rama positiva (los tests de `ner-engine` usan pools fake que nunca pasan por `handleWorkerMessage`); mismo criterio que la costura de `broadcast` (PR13/ADR-043 §4), que sí tiene sus tests. Todo lo demás del checklist (scope, spec sync, interfaces §6, eventos §7/§8, errores §11, los 10 tests con nombres exactos, casos límite §13, checklist §15, prohibiciones P-1..P-10, contratos nuevos documentados antes que el código) pasó de primera.

**Fix aplicado (2026-07-24)**: dos tests nuevos en `packages/anonymization-core/src/__tests__/unit.test.ts` (junto al test ya existente, cuyo título se acotó a lo que realmente cubre): `"un PROGRESS con onProgress registrado se lo entrega, correlacionado por jobId"` (el `jobId` se extrae del `RUN` real posteado, no hardcodeado; el spy distingue `progress`/`partial` por separado) y `"un PROGRESS de un job ya resuelto se descarta sin lanzar"` (verificado contra `worker-pool.ts:644`: `pendingRemoteJobs.delete` corre antes que `pending.resolve`, así que el `PROGRESS` tardío encuentra la entrada ya ausente).

**Revisor, segundo pase — APPROVED.** Verificó los dos tests línea por línea contra el código de `handleWorkerMessage` (confirmó que cada test falla por el motivo correcto, no de forma incidental) y corrió la suite del façade por su cuenta (93/93 verde).

**Observaciones no bloqueantes del revisor, sin acción tomada**:
- **Errata sugerida en ADR-046 §7** (ver arriba): "dos call sites" → tres, agregando `runReanalyzeOcrFlow`.
- Falta el caso negativo de correlación cruzada: ningún test verifica que un `PROGRESS` con un `jobId` **distinto** al del job pendiente no llegue a su `onProgress`. No lo pedía el ADR, pero con `nerPoolSize: 2` (dos jobs concurrentes en vuelo) sería el escenario realmente discriminante. Candidato barato para un PR futuro.
- **`modelWarm` depende de que el kernel reporte `model-ready`, y el kernel solo lo reporta cuando de verdad (re)carga el pipeline**: dos `NerEngine` compartiendo el kernel in-process ya tibio (estado a nivel de módulo) — la segunda instancia nunca recibe `model-ready`, `isModelReady()` queda en `false`. Documentado por el implementador dentro del test de paridad de `contract.test.ts` (requiere `dispose()` entre instancias). No se manifiesta en producción (cada worker carga su propio modelo, `createCore` crea una sola instancia); misma propiedad que ADR-045 dejó en `ocr-engine`.
- Item 24 de §15 ("glob de cobertura de `worker/**`"): no se agregó entrada nueva a `thresholds` porque la existente `packages/anonymization-core/ner-engine/src/**` ya cubre `src/worker/**`. Satisfecho en sustancia, dejado asentado para que no se lea como omitido.
- `poolWorkerFactories.ner` en `orchestrator.ts` queda como dead code (ningún `getPool("ner")` sobrevive), tal como ADR-046 §7 anticipó — se suma a `ocr` y `render` (ver la tarea de limpieza conjunta al final de este documento).
- `kernelDispose()` es global de módulo: `NerEngine.dispose()` libera el kernel compartido del proceso host. Espejo exacto de `ocr-engine` (ADR-045); sin consecuencia con pool real, pero es estado compartido entre instancias.
- `tests/stress/`/`tests/perf/` siguen sin archivo (caso 9 de §13, `recall`/`precision` de §14) — deuda pre-existente de todos los motores, no de este PR; el mecanismo del caso 9 queda cubierto a nivel de pool.
- Primer ciclo de import type-only `types.ts` ↔ `interfaces.ts` en `shared` (por `NerWasmPaths`): correcto bajo `verbatimModuleSyntax`/`isolatedModules` y bien comentado, pero si el patrón se repite conviene extraer los tipos compartidos a un tercer archivo.

**Estado dejado**: PR15 cerrado, revisor: **APPROVED**. Gates verdes de primera mano (corridos por el orquestador, no por el sub-agente, tras la fricción operativa de los timeouts de Bash): `pnpm lint`, `pnpm typecheck`, `pnpm test` (849 tests/71 archivos), `pnpm test:contract` (200 tests/10 archivos); cobertura `ner-engine/src` 98.93% líneas / 87.8% branch / 100% funcs, `ner-engine/src/worker` 96.07% líneas / 89.69% branch / 100% funcs — ambos muy por encima del piso de 85% (R-13). Working tree sin commitear en `feat/hito10-react-client`, a la espera de autorización explícita del humano para `git commit`.

---

## PR16 — ExportWorker (ADR-047; cerrado, revisor: APPROVED)

Auditoría del planificador (2026-07-24, mismo encargo que la de PR15). El patrón host-side ya estaba decidido, pero export tenía **cuatro forks propios** verificados contra el código: (1) **no hay transporte** — ADR-036 §1 declara el ExportWorker "sin `WorkerPool`" y `PoolKey` no tiene `"export"`, así que habría que reescribir correlación por `jobId`, `CANCEL`, `FAILED`/deserialize, crash y fallback in-process dentro del motor; además la factory se retiene hoy en el **Orchestrator** (`exportWorkerFactory`, dead code), contra el wiring de ADR-043/045/046; (2) **`ExportPagePayload` es inejecutable** — sin `imageFormat` el worker no sabe si llamar `embedJpg` o `embedPng`, y sin las dimensiones en puntos PDF no puede crear la página; (3) **el worker sí retiene estado** (el `PDFDocument` incremental) y nada especificaba qué pasa con el parcial ante otro `documentId`, cancelación o fallo; (4) **el retry podía duplicar páginas** — el guard `pageCountBeforeAttempt` deja de aplicar cuando el `pdfDoc` cruza la frontera, y un timeout sobre un mensaje que el worker sí completó produciría un PDF con la página dos veces, sin ningún error visible.

**Decisión — ADR-047** (`adr/ADR-047-ExportEngine-Ensamblador-Worker-Dedicado.md`): `export()` entero host-side; al worker cruzan dos operaciones de pdf-lib (`append-page`, `save`). El ExportWorker es un **ensamblador con estado de un documento a la vez** —la única excepción con estado de las cuatro—, con reset por `documentId` nuevo, **idempotencia por `pageIndex`** (cierra el fork 4 por construcción), descarte del parcial en `CANCEL` y limpieza tras `save`. Transporte: `WorkerPool` con `size: 1` construido por el façade e inyectado (`new ExportEngine(pool?)`, `maxRetriesOverride: 0`, normalización por `code`) — **matiza** ADR-036 §1 sin revertir su sustancia (no hay quinta clave en `WorkerPoolConfig` ni cola multi-worker; `PoolKey` gana una etiqueta interna y `WorkerPoolManager` conserva su unión de cuatro). `ExportPagePayload` se completa y la `metadata` se mueve a `ExportSavePayload`, que es donde pdf-lib la aplica.

Docs actualizados: `Export_Engine.md` v1.2.0 (cabecera, §2, §6, §12, §13 casos 18–20, §14 con 8 tests nuevos, §15 items 21–25), `05_Worker_Architecture.md` (§1 cuarta excepción, §1.1 fila del ExportWorker, §7.5 reescrito), `03_Data_Model.md` §18 (ambos payloads).

**Implementado (2026-07-29)**: `worker/assembler.ts` nuevo (a diferencia de OCR/NER, no es un kernel sin estado: expone `appendPage`/`savePdf`/`discardState` puros sobre un estado explícito `{ documentId, pdfDoc, appendedPages }` pasado/devuelto por cada función, con rollback local de página huérfana si `drawImage` falla a mitad de intento — reemplaza al guard `pageCountBeforeAttempt`); `worker/entry.ts` nuevo (`INIT`/`READY`, discrimina `append-page` vs `save` por `"pageImage" in payload`, `CANCEL` descarta el parcial, `DISPOSE`, `jobType ≠ "export-page"` → `FAILED`); `export.engine.ts` adaptado (`ExportJobPool`/`IMMEDIATE_POOL`/`constructor(pool?)`, despacho `append-page`/`save` con `maxRetriesOverride: 0`, `normalizeExportError` por `code` espejo de ADR-045/046). Costuras ajenas sancionadas por ADR-047 §2/§7: `worker-pool.ts` (`PoolKey` gana `"export"`, `ManagedPoolKey = Exclude<PoolKey, "export">` nuevo, usado por `WorkerPoolManager`); `create-core.ts` (`exportPool` de `size: 1` construido e inyectado, dispuesto en `dispose()`); `orchestrator.ts` (retiro de `exportWorkerFactory`: campo, log y comentario, y el test que lo cubría); wiring de la factory `export` en `apps/react-client`. Fuera de la lista explícita pero con precedente directo en PR14/PR15: `shared/src/types.ts`/`index.ts` (`ExportPagePayload` completado con `imageFormat`/`pageWidthPt`/`pageHeightPt` y `ExportSavePayload` nuevo — el código de `shared/` no reflejaba todavía el shape ya publicado en `03_Data_Model.md` §18); `vitest.config.ts` (alias `@anonly/export-engine` de string a `RegExp` anclado — mismo bug de resolución por prefijo que tuvo `ner-engine` en PR15). 15 tests nuevos (`worker/__tests__/entry.test.ts` + filas de `contract.test.ts`/`unit.test.ts` de `Export_Engine.md` §14).

**Ambigüedad detectada y resuelta por el implementador (no bloqueante, reportada igual)**: ADR-047 §2 cita textualmente `maxQueue: EXPORT_QUEUE_LIMIT` como si fuera una constante con fuente documentada, pero `MAX_QUEUE_PER_POOL` (`Contracts.md` §6) solo lista `{ pdf, ocr, ner, render }`, sin `export` — consistente con el propio ADR-047 §2 ("no hay quinta clave..., el façade pasa `size`/`maxQueue` como literales"). El implementador resolvió con `const EXPORT_QUEUE_LIMIT = 8` (mismo valor que ocr/ner) documentado en `create-core.ts`. El revisor confirmó independientemente que la ambigüedad es aparente (los tres docs son consistentes entre sí, nada referenciado falta) y que el valor es funcionalmente inerte con `size: 1` y despacho secuencial — no vuelve al planificador.

**Revisor: APPROVED** (gates corridos de primera mano, primer pase — el intento inicial se cortó por límite de sesión de Claude Code a mitad del checklist, sin relación con el contenido; se retomó desde la transcripción y completó el resto sin repetir trabajo). Verificó punto por punto: diff scope limpio (solo `export-engine/` + costuras sancionadas, ningún otro motor tocado); interfaces §6 exactas; los cuatro eventos sin cambios de payload/orden, blob URL creado solo en host (grep limpio en `src/worker/`); `normalizeExportError` reinstancia por `code` antes del retry en ambos loops; las 28 filas de tests §14 presentes con nombres exactos, incluidas las 8 filas nuevas de PR16 verificadas línea por línea; casos límite 18–20 cubiertos; checklist §15 items 21–25 completo; prohibiciones P-1..P-10 sin objeciones (el único `as` de producción nuevo es un cast de frontera de transporte, mismo patrón ya aprobado en render/ocr); `ExportPagePayload`/`ExportSavePayload` coinciden campo por campo con `03_Data_Model.md` §18 y ADR-047 §3.

**Observaciones no bloqueantes del revisor, sin acción tomada**:
- **Erratum sugerido en ADR-047 §2**: `maxQueue: EXPORT_QUEUE_LIMIT` ganaría claridad como `maxQueue: 8` — el nombre de constante sugiere una fuente publicada que no existe (ver ambigüedad arriba).
- **`ArrayBuffer` "transferido" es structured clone, no transfer real**: ni `entry.ts` (los cinco workers comparten el mismo helper `post()`) ni `WorkerPool.dispatchRemote` pasan transfer list, pese a que el spec y ADR-047 dicen "transferido". Deuda transversal preexistente a los cinco workers, no introducida por este PR — candidato a la limpieza conjunta ya anotada al final de este documento.
- **`save` no es idempotente ante un timeout del host** (a diferencia de `append-page`, que sí lo es por diseño de ADR-047 §4): si el host da el `save` por perdido pero el worker lo completó, el worker ya limpió su estado y el reintento choca contra `assembler.ts` con `ExportFailedError`. A diferencia del fork 4 original de ADR-047, esto falla ruidosamente (no un PDF corrupto en silencio); ventana teórica frente a un timeout de 30 s contra 500–2000 ms de `save` real. Si se quiere cerrar, la simetría natural es que el worker retenga el último `ArrayBuffer` serializado por `documentId` hasta el próximo `append-page`.
- El timeout nuevo sobre el despacho de `save` (antes de ADR-047 no tenía ninguno) produce `EXPORT_FAILED` en vez de un `EXPORT_TIMEOUT` "por página" en sentido literal de §11 — comportamiento coherente con la paridad que ADR-047 §5 persigue, anotado solo como cambio observable respecto de Hito 8.
- `WorkerPoolManager`/`ManagedPoolKey` sin objeciones: la unión de cuatro se mantiene donde importa, sin caso muerto en el `switch` de `poolSizeFor`.

**Estado dejado**: PR16 cerrado. Gates verdes de primera mano: `pnpm lint`, `pnpm typecheck`, `pnpm test` (863 tests/72 archivos), `pnpm test:contract` (203 tests), `pnpm test:security` (4 tests, no-recuperabilidad/metadata-strip por el camino nuevo). Cobertura `export-engine/src` 94.44% líneas / 86.36% branches / 100% funcs; `worker/assembler.ts` 93.57%, `worker/entry.ts` 94.17% (ambos muy por encima del piso 85%, R-13). Working tree sin commitear en `feat/hito10-react-client`, a la espera de autorización explícita del humano para `git commit`.

---

## PR17 — E2E completa (ADR-048; cerrado, revisor: APPROVED — 3 escenarios en `fixme` justificado)

Auditoría del planificador (2026-07-24). Cuatro de los ocho escenarios asignados a PR17 no son ejercitables hoy, y hay un problema que afecta a **toda** la suite:

1. **La suite E2E no puede pasar en CI — ni la que ya existe.** Los tres directorios de assets first-party están gitignoreados por ADR-018 (`public/wasm/` 7,7 MB, `public/models/` 179 MB, `src/assets/onnxruntime/`) y el job `test-e2e` de `ci.yml` **nunca corre `pnpm assets:mirror`**. El tercero es un `import … ?url` que Vite tiene que resolver: sin él, el dev server que levanta Playwright no sirve una app funcional y **se cae la suite entera**, no solo NER/OCR. Los "gates verdes" reportados en PR10/PR13/PR14 fueron **locales**, con el mirror ya corrido; el job de CI nunca se verificó con tests presentes. Un gate declarado activo que no puede pasar enmascara regresiones de los PRs 12–16.
2. **Fixtures**: solo se generan 3 de los 8 del README. Escenario 4 no necesita `huge-1000p.pdf` (el generador de `many-Np.pdf` de `viewer-scroll-jump.spec.ts` alcanza); Escenario 2 se resuelve generando el escaneado **en el browser** (rasterizar `text-10p.pdf` con el pdfjs que la app ya carga + re-armar con pdf-lib) — lo que supersede el diferimiento a Hito 11 anotado en la entrada "PR14"; Escenario 3 **no tiene solución con las deps actuales**: pdf-lib no encripta.
3. **Escenario 7 pisa el gate `test:leak`** de Hito 11 palabra por palabra, y medir memoria en Playwright exige `crossOriginIsolated` (COOP/COEP), headers que PR10 descartó. Reparto: el E2E ejercita el flujo y la limpieza observable; la métrica queda en `tests/leak/`.
4. **Escenario 8 sigue en `fixme`** con la pregunta del implementador sin responder desde PR10: no hay wiring `settings.store` → `EngineConfig` (`App.tsx` llama `initCore()` sin overrides, aunque `initCore` ya los acepta desde ADR-039). No es solo un test bloqueado: el toggle de NER sin documento abierto **no hace nada**, contra lo que promete `React_Client.md` §3.7.
5. Deudas menores que PR17 absorbe: `tests/e2e/` fuera de la cadena de `pnpm typecheck`; comentario stale en `scenario-1` sobre la visibilidad de `ExportButton`.

**Decisión — ADR-048** (`adr/ADR-048-Cierre-E2E-Hito10-Fixtures-Assets-Escenarios.md`): mirror obligatorio en el gate E2E (CI con cache por hash de `assets.lock.json`) y verificación explícita del diagnóstico como primer item del PR; fixtures generados en runtime (Node o browser), cero binarios nuevos; reparto del Escenario 7; orden interno del PR. Docs: `07` §11.3/§11.4, `tests/fixtures/README.md`.

**Los dos puntos de alcance quedaron ratificados por el humano (2026-07-24), opción recomendada en ambos**:
- **Escenario 3**: `protected.pdf` **se commitea** (~100 KB, generado una sola vez con `qpdf --encrypt test1234 test1234 256 -- text-10p.pdf protected.pdf`). No lo cubre ADR-018 —eso rige los assets first-party mirroreados, no los fixtures de test—; queda documentado en `tests/fixtures/README.md` como el único fixture commiteado, con su comando de reproducción.
- **Escenario 8**: se crea **PR16.5** (ver la entrada de abajo). PR17 lo saca del `fixme`.

Con eso, los ocho escenarios de PR17 arrancan sin bloqueo.

**Implementado (2026-07-30)**: los cinco gates locales (`lint`, `typecheck`, `test` 871/73, `test:contract` 203/10, `test:e2e`) verdes. `.github/workflows/ci.yml` (job `test-e2e`): `actions/cache` sobre los tres directorios de assets first-party (clave `hashFiles('assets.lock.json')`) + paso `pnpm assets:mirror`, ambos con el mismo `if` condicional que los pasos vecinos. `package.json` raíz: `typecheck` gana `tsc -p tests/e2e/tsconfig.json --noEmit`. Comentario stale de `scenario-1-import-edit-export.spec.ts` corregido (`ExportButton` se gatea por `{Ready, Done}`, no solo `Ready`). Specs nuevos: `scenario-4-cancel-huge-pdf.spec.ts`, `scenario-5-edit-during-ner.spec.ts`, `scenario-9-ner-runtime-reanalyze.spec.ts`, `scenario-10-merge-split-groups.spec.ts`, `scenario-11-zoom.spec.ts` (verdes); `scenario-8-ner-disabled.spec.ts` sacado de `fixme` (verde, desbloqueado por PR16.5). `tests/fixtures/protected.pdf` commiteado (único binario nuevo, AES-256/R6 vía `qpdf`, password `test1234`, no gitignoreado, `qpdf --check` limpio — 4.3 KB reales, no los ~100 KB estimados del README).

**Tres escenarios quedaron en `test.fixme`, verificados por el revisor uno por uno contra el código — ninguno resuelto tocando `packages/`**:
- **Escenario 2 (OCR)** y **Escenario 3 (PDF protegido)**: bugs reales de Core, fuera de alcance de este PR. Ver la entrada de abajo ("Bug destapado durante PR17") para el Escenario 3 (ADR-049, PRs 17.1/17.2 ya en curso). El Escenario 2 (`TESSERACT_WORKER_PATH` apuntando a un directorio en `ocr-engine/src/worker/kernel.ts:44-46` en vez de al archivo real) quedó reportado vía `spawn_task`, sin ADR todavía.
- **Escenario 7 (abrir/cerrar 10 documentos)**: no es un bug — es una **contradicción real con ADR-048 §3**, confirmada independientemente por el revisor (`actions.closeDocument()` solo se invoca desde `PasswordDialog.tsx:153` y `PipelineStatus.tsx:65`, y este último exige `stage === Failed`; no existe ningún control de UI para cerrar un documento en `Ready`, y `orchestrator.ts#validateImportInput` exige ese cierre antes de la próxima importación). **Decisión pendiente del planificador** — el revisor recomienda un PR nuevo de `apps/react-client` que agregue un control "Cerrar documento" al `Toolbar` (documentándolo primero en `Components.md`/`React_Client.md`), notando que el gate `test:leak` de Hito 11 va a chocar con la misma pared si no se resuelve.

**Revisor: APPROVED** (gates corridos de primera mano en WSL, incluido `test:e2e`: 9 passed / 3 skipped / 0 failed, 42.9s — no reprodujo la falla pre-existente que el implementador había reportado como ajena al PR). Confirmó diff scope limpio (cero cambios bajo `packages/`), los tres diagnósticos de `fixme` verificados línea por línea contra el código, la config de CI, el `tsconfig` de typecheck, `protected.pdf` verificado con `qpdf --check`, y las prohibiciones P-1..P-10 sin objeciones.

**Observaciones no bloqueantes del revisor, sin acción tomada**:
- Falta `tests/e2e/README.md` con el prerequisito `assets:mirror` — único item de ADR-048 no entregado (el prerequisito sí quedó documentado en `07` §11.4 y `tests/fixtures/README.md`, así que no se perdió información).
- La aserción de "Teléfono" del Escenario 8 fija un falso positivo: el CUIT del fixture falla su propio checksum AFIP (confirmado, dígito verificador 6 no 9) y el motor lo descarta bien, pero el valor que matchea como "teléfono" (`20-12345678`) son los 10 primeros dígitos del CUIT rechazado — el teléfono real del fixture (`+54 11 1234-5678`) no aparece. La especulación del comentario del implementador sobre la causa (interacción entre patrones cercanos en `resolveOverlaps`) no se sostiene simulando la resolución real; la causa está más arriba (extracción de texto o mapeo span→`Word[]`), sin investigar. Recomendación: quitar esa aserción (DNI + Email ya cubren el punto del test) y abrir aparte "un teléfono real del fixture de referencia no se detecta".
- `tests/fixtures/README.md` ("entidades esperadas") desactualizado más allá del CUIT: con NER off el pipeline tampoco detecta IBAN ni tarjeta de la página 2 — insumo para el dataset de referencia del Hito 11.
- Escenario 4 no mide el SLA de 200 ms (diferido a `tests/cancel/`, mismo criterio que el reparto del Escenario 7), pero `07` §11.3 item 4 no tiene la nota de reparto que sí recibió el item 7 — convendría anotarla para que el spec no se lea como incumplido.
- El `tsconfig` de ADR-048 §6 pedía "referencia desde el tsconfig raíz"; el implementador agregó un `tsc -p` directo al script — equivalente y más consistente con cómo ya se tratan `tests/`/`scripts/`, sin objeción.

**Estado dejado**: PR17 cerrado. Gates verdes de primera mano. Working tree sin commitear en `feat/hito10-react-client`, a la espera de autorización explícita del humano para `git commit`.

---

## PR16.5 — wiring `settings.store` → `EngineConfig` en el bootstrap (ADR-048 §7 punto 2; cerrado, revisor: APPROVED)

PR chico de `apps/react-client`, **entre PR16 y PR17**. Cierra la pregunta que el implementador dejó abierta en PR10 y un bug de producto real: hoy `nerEnabled: false` persistido antes de la primera importación no tiene **ningún** efecto observable — `App.tsx` llama `initCore()` sin argumentos una sola vez por carga de pestaña, y `core-adapter` nunca deriva un `EngineConfig` desde `settings.store`, aunque `React_Client.md` §3.7 promete que "afecta al próximo `createCore`".

Alcance exacto (todo lo demás ya existe): `settings.store.load()` ya persiste `nerEnabled`/`ocrLanguages`/`performancePreset`, y `initCore` ya acepta `EngineConfigOverrides` desde ADR-039 — **falta solo el llamador**. El bootstrap carga los settings y pasa los overrides derivados según `React_Client.md` §3.7 (actualizado por este ADR para cubrir el bootstrap, no solo el flujo con documento abierto): `nerEnabled → ner.enabled`, `ocrLanguages → ocr.languages`, `performancePreset → workerPool.*PoolSize` con `auto` = **omitir** la sección (no pisar los defaults derivados de `hardwareConcurrency`). El override de `ner.wasmPaths` que `initCore` ya inyecta (ADR-039) gana siempre: los del usuario se mergean por debajo. Fuera de alcance: recrear el core al cambiar un setting con documento abierto (sigue siendo `reanalyze`, ADR-038 §7) y el redimensionado de pools en caliente.

**Implementado (2026-07-30)**: `core-adapter/settingsToEngineConfig.ts` nuevo — función pura `deriveEngineConfigOverrides(settings)` que aplica la tabla de `React_Client.md` §3.7 (`nerEnabled → ner.enabled` y `ocrLanguages → ocr.languages` directos e incondicionales; `performancePreset` vía `switch` exhaustivo sobre los tres presets, con `auto` devolviendo un objeto sin la clave `workerPool` — tipado con una unión `{ workerPool: WorkerPoolSizes } | Record<string, never>` y spread condicional, no `undefined` explícito, compatible con `exactOptionalPropertyTypes`). `App.tsx`: en el único `useEffect` de montaje, `useSettingsStore.getState().load()` → deriva el override → `initCore(overrides)`, en ese orden. `core-adapter/index.ts`: solo el docstring de `initCore` actualizado (ya no describe el gap que este PR cierra). 8 tests nuevos en `__tests__/settings-to-engine-config.test.ts` (los tres presets, con aserción explícita `"workerPool" in overrides === false` para `auto`; mapeo directo de `nerEnabled`/`ocrLanguages`).

Sin ambigüedad detectada: `React_Client.md` §3.7, esta entrada y ADR-048 §7 punto 2 coincidían entre sí. Decisión de convención (no ambigüedad) tomada por el implementador: el archivo nuevo vive en `core-adapter/` (no en `store/`) porque deriva el input que `initCore` necesita, acoplado a `EngineConfigOverrides` y consumido solo desde el propio bootstrap.

**Revisor: APPROVED** (gates corridos de primera mano en WSL). Verificó los cuatro archivos del diff acotados a `apps/react-client/` (sin tocar `packages/`); el mapeo de los tres presets contra `React_Client.md` §3.7 línea por línea, incluida la omisión real de `workerPool` en `auto` (no `{}` anidado); que `mergeEngineConfig` conserva `maxQueuePerPool`/`timeouts`/`maxRetries` de los defaults al aplicar `low`/`high` (solo pisa los 4 tamaños); que el override de `ner.wasmPaths` de ADR-039 sigue ganando (la función derivadora nunca emite esa clave); el orden `load()` → derivar → `initCore()` dentro del mismo efecto; los 8 tests nuevos; conteo global exacto (871/73, +8/+1 sobre el baseline de PR16); prohibiciones P-1..P-10 sin objeciones; `pnpm test:contract` idéntico al baseline (203 tests, confirma que nada del Core cambió).

**Observaciones no bloqueantes del revisor, sin acción tomada**:
- Comentario interno en `settingsToEngineConfig.ts:63-64` con una justificación técnicamente incorrecta: afirma que `{ workerPool: undefined }` "pisaría los defaults en el merge", pero `mergeEngineConfig` hace spread (`{ ...base.workerPool, ...overrides.workerPool }`) y difundir `undefined` es un no-op — el comportamiento implementado es correcto, la razón documentada en ese comentario puntual no. Candidato a un fix de una línea.
- `SettingsDialog.tsx:20-23` conserva un docstring que describe el gap "core-adapter no deriva `EngineConfig` de `settings.store`, heredado de PR5" como si siguiera abierto — es exactamente el gap que cierra este PR. No tocado por el diff (archivo fuera de alcance).
- `React_Client.md` §2 (árbol de `core-adapter/`) quedó desactualizado: lista 4 archivos, ahora hay 5. R-21 lo deja para el planificador, no para un PR de implementación.
- Sin test del invariante de orden (`load()` antes de `initCore`) a nivel de `App.tsx`: el repo no tiene infraestructura de render de componentes (`vitest.config.ts` usa `environment: node`), así que no es testeable con el setup actual — descansa en revisión de código.
- `PasswordDialog.tsx:51` tiene un segundo call site de `initCore()` sin argumentos, preexistente y sin cambios en este PR. Inofensivo (`initCore` es idempotente y el efecto de montaje de `App` corre primero), pero "una sola llamada" no es literal en el código, solo en la práctica.
- Naming mixto en `core-adapter/`: `settingsToEngineConfig.ts` es camelCase mientras sus hermanos son kebab-case (`bus-bridge.ts`); no es violación de `Code_Standards.md` (camelCase tiene precedente en `apps/`, p. ej. `reanalyzePlan.ts`), solo inconsistencia interna.

**Estado dejado**: PR16.5 cerrado. Gates verdes de primera mano: `pnpm lint`, `pnpm typecheck`, `pnpm test` (871 tests/73 archivos), `pnpm test:contract` (203 tests, sin cambios respecto de PR16). Desbloquea el Escenario 8 de PR17. Working tree sin commitear en `feat/hito10-react-client`; el humano autorizó el commit de PR16 + PR16.5 antes de empezar PR17 (2026-07-30).

---

## Bug destapado durante PR17 — `PdfPasswordRequiredError` pierde su identidad de clase al cruzar el Worker (ADR-049; cerrado, revisor: APPROVED)

**Encontrado y rastreado por el implementador** mientras escribía el Escenario 3 (PDF protegido), con `tests/fixtures/protected.pdf` ya commiteado (ADR-048 §7 punto 1). Reproducible 2/2, con script standalone y con el propio spec. No es un problema del test: **en la app real, hoy, un PDF protegido es indistinguible de uno corrupto** — se ve el banner genérico de pipeline fallido con "Cerrar documento" en vez del `PasswordDialog`, que alcanza a abrirse por el evento y se cierra solo al pasar el stage a `Failed`.

Causa raíz (verificada línea por línea por el planificador antes de decidir): `serialize()`/`deserialize()` no transportan el prototipo. El worker postea `FAILED` con `err.serialize()`, `worker-pool.ts` reconstruye con `EngineError.deserialize()` un `DeserializedEngineError` genérico, y el `err instanceof PdfPasswordRequiredError` de `handleExtractionFailure` da `false` → `failPipeline`. Los **eventos** sí cruzan intactos (ADR-013 §6), solo el camino de excepción pierde fidelidad; por eso el diálogo llega a abrirse. Invisible con pools in-process (Hito 9) y en los unit tests del façade, que mockean los motores y nunca serializan: **los tests actuales pasan con el bug vivo**.

Aportes del planificador sobre el reporte original: (a) hay un **segundo** sitio afectado, el `isRetryable` propio del despacho de `pdf-parse`, cuyo `!(err instanceof PdfPasswordRequiredError)` también falla — como `PdfPasswordRequiredError.retryable` sigue en `true`, el pool **reintenta** el PDF protegido con backoff antes de fallar; (b) barrido del resto de los `instanceof`: `CancelledError` **no** está afectado (el transporte tiene un frame `CANCELLED` propio y el host instancia el error localmente), `isRetryable`/`isTimeoutError` del pool tampoco (usan `instanceof EngineError` + flag/`code`, que sí sobreviven), y los tres motores con pool propia ya discriminan por `code` en sus host-bridges — el Orchestrator era el único consumidor que discriminaba por subclase concreta; (c) la mitad del fix ya estaba planificada: el `retryable = false` que ADR-035 §3 dejó como "PR chico posterior al Hito 9" resulta ser la otra mitad de este bug, y dejó de ser no bloqueante.

**Decisión — ADR-049** (`adr/ADR-049-Errores-Cruzando-Worker-Discriminacion-Por-Code.md`): se discrimina por `err.code`, nunca por `instanceof <SubclaseConcreta>`; `EngineError.deserialize()` **no se toca** (un registry `code → constructor` invertiría capas —`shared` no puede conocer las clases de los motores, P-1/P-2— y además sería lossy: los constructores toman argumentos de dominio, así que reconstruir descartaría el `message`/`details` que realmente viajaron); type-guard `isEngineErrorCode` en el façade; `PdfPasswordRequiredError.retryable → false` y retiro del override. **Ningún contrato público cambia** — `Contracts.md` §4 solo precisa la redacción del comentario de `deserialize()`. Docs actualizados: `Code_Standards.md` §7 (regla normativa nueva, con el ejemplo malo y el bueno), `Contracts.md` §4, `Orchestrator.md` v1.5.2 (§13 caso 3, §14 dos tests, §15 item 19), `PDF_Engine.md` §11/§14/§15 item 21, `05_Worker_Architecture.md` §5, `MVP.md`, `ADR-038` §8.

**Dos PRs, orden obligatorio**: **17.1** (`pdf-engine`, tres líneas + test) antes de **17.2** (façade: guard por `code`, retiro del override y del import huérfano, tests con el error **deserializado**, y el des-`fixme` del Escenario 3 como evidencia). Invertirlos deja el override retirado con `retryable: true` y el pool reintentando. El Escenario 3 viaja en el 17.2 y no en PR17 porque es el test que prueba el fix; PR17 conserva los otros siete escenarios.

**Implementado (2026-07-30)**: **PR17.1** — `pdf-engine/src/pdf.errors.ts`: `PdfPasswordRequiredError` construye con `retryable: false` (única línea de producción); test `PdfPasswordRequiredError is not retryable` en `edge.test.ts`. **PR17.2** — `packages/anonymization-core/src/errors.ts`: `isEngineErrorCode<C>()` nuevo, verbatim de ADR-049 §3; `orchestrator.ts#handleExtractionFailure` discrimina con `isEngineErrorCode(err, EngineErrorCode.PDF_PASSWORD_REQUIRED)`; retirado el override de `isRetryable` del despacho `pdf-parse` y el import huérfano de `PdfPasswordRequiredError`; dos tests nuevos en `edge.test.ts` del façade que inyectan el error **deserializado de verdad** (`EngineError.deserialize(new PdfPasswordRequiredError(id).serialize())`) — el punto central del ADR, ya que los tests con la clase concreta pasan con el bug vivo. `tests/e2e/scenario-3-protected-pdf.spec.ts` sacado de `fixme` con aserciones reales (viaja en 17.2 por ser la evidencia, ADR-049 §7).

**Revisor: APPROVED los tres** (17.1, 17.2, y PR17.3 de abajo, revisados juntos por estar relacionados). Gates de primera mano: `pnpm lint`/`typecheck` verdes, `pnpm test` 877/74, `pnpm test:contract` 203/10, cobertura sin fallos de threshold (`src/errors.ts` 100%, `pdf.errors.ts` 100%, `orchestrator.ts` 91.48%). Verificó línea por línea: el diff de 17.1 es mínimo; en 17.2 confirmó por grep que no queda ningún `instanceof` de subclase concreta salvo `CancelledError` (exento); que los dos tests nuevos son guardas genuinas (rompen si se revierte el `instanceof` o si se revierte PR17.1); que el estado final combinado es el que ADR-049 describe (nunca coexiste el override retirado con `retryable: true`); y corrió el Escenario 3 él mismo, confirmando que falla en la línea 77 por un bug **distinto y no introducido por este lote** (ver "PR17.3" abajo).

**Observaciones no bloqueantes del revisor, sin acción tomada**:
- Comentario stale en `packages/anonymization-core/src/worker-pool.ts:156-164` (JSDoc de `DispatchParams.isRetryable`): todavía dice que `PdfPasswordRequiredError` se marca `retryable: true` y que `pdf-parse` pasa un override — ambas afirmaciones las volvió falsas este mismo lote. Era in-scope de PR17.2 (riesgo cero de arreglar ahí) y es justo la trampa para el próximo lector que ADR-049 quiere evitar. Candidato a fix de una línea.
- `shared/src/errors.ts:54-55` (`EngineError.deserialize`) sigue con el comentario viejo ("los motores específicos pueden override...") pese a que `Contracts.md` §4 ya tiene la redacción corregida — divergencia doc/código sin impacto de comportamiento; corregirla es un PR de `shared/` aparte por R-1 (probablemente por eso ADR-049 no lo incluyó).
- Checkboxes `[ ]` sin marcar en `Orchestrator.md` item 19 y `PDF_Engine.md` item 21 pese a estar implementados — patrón de bookkeeping preexistente (el item 20 de `PDF_Engine.md`, de PR12 ya aprobado, tiene el mismo problema).

**Estado dejado**: PR17.1 y PR17.2 cerrados. Working tree sin commitear en `feat/hito10-react-client`, a la espera del commit del lote completo (PR17 + ADR-049 + 17.1 + 17.2 + 17.3).

---

## PR17.3 — fix de race condition en `initCore()` (`apps/react-client`; sin ADR propio, autorizado por el humano; cerrado, revisor: APPROVED)

Encontrado por el implementador de PR17.2 al intentar poner en verde el Escenario 3 tras el fix de ADR-049: el façade funcionaba (verificado inyectando el error real), pero `PasswordDialog` nunca se abría en el DOM. Causa: `apps/react-client/src/core-adapter/index.ts#initCore()` cachea el **valor ya resuelto** (`if (core) return core`), no una promesa en vuelo. `App.tsx` (con los overrides de `settings.store`, PR16.5) y `PasswordDialog.tsx` (sin overrides) llaman `initCore()` cada uno desde su propio `useEffect` de montaje; en React los efectos de los **hijos** corren antes que los del padre, así que `PasswordDialog` entra primero — si ambos efectos corren antes de que el primer `createCore()` resuelva (crea 7 motores + pools, no es instantáneo), se crean **dos instancias** de `IAnonymizationCore`. La última en resolver gana la variable de módulo; el otro caller queda suscripto al bus de una instancia huérfana. Confirmado determinístico, 4/4 corridas.

**Trampa identificada por el planificador antes de asignar el fix**: un fix ingenuo de "cachear la promesa" arregla la duplicación de instancias pero dejaría que `PasswordDialog` (sin config) gane la carrera de inicialización siempre, porque sus efectos corren primero — el `initCore(overrides)` de `App.tsx` quedaría recibiendo la misma promesa ya iniciada, **ignorando en silencio sus propios overrides**. Eso reintroduciría el bug que PR16.5 cerró, pero de forma silenciosa e indetectable en vez de con el fallo ruidoso actual.

**Implementado (2026-07-30)**: separación de potestades en `core-adapter/index.ts`: `initCore(config?)` sigue siendo la única función que puede invocar `createCore()` (guardada por un flag síncrono `creationStarted`, sin ventana de carrera posible antes del primer `await`); `getCoreAsync()` nueva, de solo lectura, espera la instancia ya en curso o resuelta **sin poder influir en su config**. `PasswordDialog.tsx` migra de `initCore()` a `getCoreAsync()` — deja de tener potestad para arrancar el core. `disposeCore()` resetea el estado nuevo para que un `getCoreAsync()` posterior no cuelgue de una promesa vieja; el camino de reintento ante un `createCore()` fallido se preserva. Test nuevo `core-adapter-init-precedence.test.ts` (3 casos) que prueba explícitamente la **precedencia de config bajo carrera** (no solo identidad de instancia): simula `getCoreAsync()` ganando el efecto antes que `initCore(overrides)`, y verifica que `createCore` se llama una sola vez **con el config de `App.tsx`** (`ner.enabled`/`ner.wasmPaths` incluidos) — la aserción exacta que un fix de "cachear la primera promesa" no pasaría. `React_Client.md` §2.1 documenta `getCoreAsync()` (nota aditiva, autorizada explícitamente sin ADR por no cambiar ningún contrato de motor).

**Revisor: APPROVED** (mismo pase que 17.1/17.2, ver arriba). Verificó que la guarda de `initCore` es síncrona antes del primer `await` (sin ventana de carrera real); que ningún otro call site de `apps/react-client/src/` puede ganar la carrera (`grep initCore(` da un solo invocador con config, `App.tsx:53`); y, con escepticismo explícito pedido, leyó el test de precedencia línea por línea y confirmó que sí verifica qué `config` recibió `createCore.mock.calls[0]`, no solo identidad de instancia.

**Escenario 3 E2E — avanza pero no llega a verde, por un bug nuevo y distinto, fuera de alcance**: con el race arreglado, el diálogo abre sobre el evento real y el flujo de contraseña incorrecta/correcta funciona. Pero el pipeline falla más adelante, en Render, con `"No password given"`. Causa (verificada por el implementador y confirmada por el revisor, sin tocar código): `orchestrator.ts#retryWithPassword` (líneas ~295-308) arma el input con la contraseña solo para la re-extracción de `PdfEngine`, pero **nunca actualiza `this.retainedInputs`** — y `ensureRenderDocumentLoaded` (líneas ~701-711) lee ese mismo `retainedInputs` (sin contraseña) y le pasa a `RenderEngine.loadDocument()` los bytes todavía encriptados. `render-engine` no tiene ningún soporte de contraseña hoy. Cruza `orchestrator.ts` + el contrato de `render-engine`: probablemente necesita ADR propio. Quedó como chip de tarea aparte (no asignado todavía) en vez de mezclarse en este PR.

**Observaciones no bloqueantes del revisor**:
- Rechazo de `createCore()` posiblemente no observado si nadie llamó `getCoreAsync()` todavía (`unhandledrejection` en consola) — sin test que lo dispare, sin impacto en gates.
- `disposeCore()` abandona incondicionalmente el deferred anterior — solo alcanzable desde tests, no es contrato de UI.
- PR17.3 no tiene fila propia en `MVP.md` (sí 17.1/17.2) ni entrada previa en este documento — consistente con haber sido autorizado sin ADR, pero quedaba sin trazabilidad hasta esta entrada.

**Estado dejado**: PR17.3 cerrado. Gates de primera mano compartidos con el pase de 17.1/17.2 (ver arriba). Working tree sin commitear, a la espera del commit del lote completo.

---

## Bugs destapados al cerrar el lote de PR17 (2026-07-30) — tres, de tres naturalezas distintas (cerrado, revisor: APPROVED)

Los tres los encontró el implementador al terminar PR17/17.1/17.2/17.3, y en los tres paró antes de tocar código, correctamente.

### 1. El password no llega a `RenderEngine.loadDocument` — **ADR-050**

Al desbloquear el flujo de password (ADR-049), el Escenario 3 avanzó más lejos y murió en el paso siguiente: `RenderFailedError("No password given")` → el mismo banner genérico, causa nueva, **después** de que el usuario ingresara la contraseña correcta.

Verificado por el planificador contra el árbol post-`e277b0f`: son **dos** defectos. (a) `retryWithPassword` arma `retryInput` como variable local y nunca reescribe `retainedInputs` — el commit de PR17.2 no lo tocó, así que el fix del façade **sigue pendiente**, no se hizo de paso. (b) Aunque (a) estuviera arreglado, `loadDocument(documentId, buffer)` no tiene tercer parámetro y `LoadDocumentPayload` no lleva password: no habría por dónde. Además: **no depende del transporte** (falla igual con pools in-process; el agujero está abierto desde ADR-030 y nunca se ejercitó porque el Escenario 3 estuvo en `fixme` desde PR10) y **no es solo el preview** — rompe los tres caminos que pasan por `ensureRenderDocumentLoaded`: rasterización para OCR, seed del preview (ADR-044) y export en `mode: "full"`.

Necesitaba ADR no tanto por la firma como por `08_Security_Model.md` §6, que es normativo y decía que el password vive "solo en el payload del `pdf-parse` job" y "no se persiste en ningún lado", con un grep de CI (§6.3) que se pondría rojo. **Decisión — ADR-050, opción A ratificada por el humano (2026-07-30)**: `loadDocument(documentId, buffer, password?)` + `LoadDocumentPayload.password`; el password se retiene **solo host-side** (Orchestrator para re-correr el pipeline, Render host para re-primear workers — ADR-043 §5) y el **kernel no lo guarda**; §6 se enmienda con una tabla explícita de dónde vive el secreto y hasta cuándo, y el grep gana una excepción enumerada. Descartadas: no retener nada (rompe el re-priming: preview muerto en silencio tras un crash de worker) y descifrar en `pdf-engine` (depende de una capacidad no verificada de pdfjs, cambia `PdfEngineOutput`, duplica el documento en RAM y no mejora nada materialmente). **PRs 17.4** (`render-engine` + `shared`) → **17.5** (façade), en ese orden.

**Implementado (2026-07-30)**: **PR17.4** — `render-engine/src/render.engine.ts`: `loadDocument(documentId, buffer, password?)`, `RetainedDocument.password`, `reprimeWorkers()` reenvía el password retenido; `worker/kernel.ts` lo pasa a `getDocument({data, password})` sin retenerlo; `shared/src/types.ts`: `LoadDocumentPayload.password`. Tres tests nuevos con nombres exactos del §14 (`Render_Engine.md` v1.6.0, caso 22). **PR17.5** — `orchestrator.ts#retryWithPassword` ahora reescribe `this.retainedInputs` (no solo una variable local); `ensureRenderDocumentLoaded` pasa `retained.password` a `loadDocument`. Tres tests nuevos de façade (`Orchestrator.md` §14) con nombres exactos. Escenario 3 E2E cerrado de punta a punta: contraseña incorrecta reintenta, correcta cierra el diálogo, **y ahora el preview se pinta de verdad** (verificado con lectura de pixels del canvas, no solo ausencia de banner).

**Revisor: APPROVED** (17.4, 17.5, y los otros dos de esta entrada, un solo pase — ver "Estado dejado" al final). Verificó el reparto del secreto en código (`RetainedDocument.password` solo host-side, kernel no lo retiene en ninguna variable de módulo, borrado por los `documents.delete`/`.clear` ya existentes), los tres tests de §14 con nombres exactos, y corrió el Escenario 3 tres veces con verificación de bitmap real. Observación no bloqueante: el grep de `08_Security_Model.md` §6.3 sigue sin existir como script/test ejecutable en CI (documentado, no automatizado) — verificado a mano por implementador y revisor, limpio para este lote, pero con dos huecos en la lista enumerada del ADR (`ImportDocumentInput.password`/`retryWithPassword` en `src/types.ts`, y el filtro de grep no cubre los identificadores PascalCase de `PdfPasswordRequiredError`) que habría que cerrar el día que se implemente de verdad.

### 2. `TESSERACT_WORKER_PATH` apunta a un directorio — sin ADR, es una errata

`ocr-engine/src/worker/kernel.ts:46` define `TESSERACT_WORKER_PATH = "/wasm/tesseract/"`. tesseract.js hace `importScripts(workerPath)` sin agregar nombre de archivo → **falla en silencio**: cualquier PDF escaneado llega a "Listo" con 0 entidades, sin error ni evento de fallo. Verificado contra `assets.lock.json`: el archivo se mirrorea en `public/wasm/tesseract/worker.min.js`, así que el valor correcto es esa ruta completa. `corePath` y `langPath` **no** tienen el problema: son directorios legítimos (el lock mirrorea dos variantes de core justamente porque tesseract.js elige una ahí adentro).

No es cambio de contrato: es una constante mal apuntada, heredada de la línea de `ADR-018` §2 que listaba las tres rutas como si las tres fueran directorios. Anotada como errata en `ADR-018` §2 y en `OCR_Engine.md` v1.2.1 + item §15.22. **PR 17.6** (`ocr-engine`). Detalle que el PR no puede saltear: el Escenario 2 E2E hoy pasa **en verde** con el OCR roto, porque afirma el stage y no el resultado — tiene que afirmar entidades > 0.

**Segunda mitad, encontrada al implementar el PR 17.6** (diagnóstico del implementador, verificado con Playwright real): el fix de la constante arregla el **fallback in-process** y no el camino real de producción. `tesseract.js@6` solo absolutiza sus rutas si el entorno es `'browser'`; dentro del OcrWorker es `'webworker'`, y con `workerBlobURL: true` (default) el worker interno que crea corre con `self.location = blob:<origen>/<uuid>` — contra una base `blob:` un path root-relative **no resuelve** (`new URL("/wasm/…", "blob:…")` lanza `Invalid URL`), sea archivo o directorio.

**Sigue sin ADR** (verificado por el planificador): el fix vive entero en `ocr-engine/src/worker/kernel.ts`, no toca ningún contrato público, no agrega dependencias y —punto que sí se chequeó explícitamente— **no requiere tocar la CSP**: `script-src`/`worker-src` ya admiten `blob:` desde PR10 (`08` §3.2) y ninguna de las dos opciones pide una directiva nueva. Tampoco aplica el precedente de ADR-039 (sacar las rutas a config inyectada por la app): ahí hacía falta porque los assets de ort son módulos hasheados por Vite que solo la app conoce; los de tesseract son archivos estáticos en `public/` con ruta fija, así que el motor puede resolverlos solo.

**Decisión (planificador, 2026-07-30)**: absolutizar las tres rutas contra `self.location.origin` en el kernel, con fallback a la ruta root-relative si `self.location` no existe (node/tests). Es correcto en los dos caminos (in-process y worker), no depende de flags internos de tesseract y sigue siendo first-party — cambia la forma de la URL, no el destino (`ADR-018` §2, precisión agregada). `workerBlobURL: false` queda documentada como **palanca de reserva**, no como fix primario: elimina la base `blob:` de raíz —y con ella el riesgo del punto 4 sin verificar (`getCore.js`)— pero es un flag de la librería y no arregla la causa (las rutas siguen sin absolutizar). Si tras absolutizar aparece igual una resolución interna root-relative, se agrega y se documenta el motivo. Spec: `OCR_Engine.md` v1.2.2 + item §15.22 reescrito.

**Auditoría de los otros tres motores** (misma clase de bug, resultado negativo): ninguno lo sufre hoy, y por un motivo verificable — el suyo es un worker con URL `http` real, así que cualquier ruta root-relative resuelve contra el origen. `ner-engine` usa `env.localModelPath = "/models/ner/"` dentro del NerWorker y funciona; el worker de inferencia de ort **sí** nace de un blob, pero recibe su `.mjs` ya resuelto por Vite (`?url`, inyectado por la app vía `NerConfig.wasmPaths`, ADR-039) y lo fetchea **antes** de armar el blob. `pdf-engine`/`render-engine` configuran `GlobalWorkerOptions.workerSrc` con una URL de Vite, misma situación. La diferencia de tesseract es que le pasamos la ruta cruda **hacia adentro** del contexto blob. Vale re-chequear si alguna de esas librerías cambia de estrategia de carga.

**Implementado (2026-07-30)**: `worker/kernel.ts` — `TESSERACT_WORKER_PATH` corregido al archivo real, y `resolveTesseractPath()` nuevo que absolutiza `langPath`/`corePath`/`workerPath` contra `self.location.origin` (no-op si `self.location` no existe). `workerBlobURL: false` **no** se usó, queda solo como comentario de palanca de reserva. Dos tests nuevos en `worker-entry.test.ts` (uno por cada parte del fix, cada uno verificado fallando con el fix revertido). Escenario 2 E2E sacado de `fixme`: aserción central de entidades reales (`role="tree"` + `treeitem` count > 0 + el DNI conocido de la página 0), no el `stage` — corrido 5 veces en Chromium real por el implementador (3 aislado + 2 en serie, igual que CI) y 3 veces más por el revisor, siempre verde.

**Revisor: APPROVED.** Confirmó que `corePath`/`langPath` siguen siendo directorios legítimos (no tocados en la parte a) y se absolutizan igual en la parte b; que la CSP no necesitó tocarse (`script-src`/`worker-src` con `blob:` ya desde PR10); y coincidió en que la decisión "sin ADR" es razonable — el fix vive en un solo archivo de un motor, sin cambio de contrato, con la tabla de alternativas y el motivo del descarte de `workerBlobURL: false` ya escritos en `OCR_Engine.md`/`ADR-018` **antes** que el código. Observación no bloqueante: el comentario de `kernel.ts` sobre cuándo `resolveTesseractPath` es no-op es impreciso (dice "sin `self`/`self.location`" pero en el hilo principal de un browser `self === window` sí tiene `location` y absolutiza igual — el no-op estricto solo aplica al entorno Node de los tests); mismo texto replicado en el header del spec E2E.

### 3. No hay forma de cerrar un documento cargado con éxito — decisión abierta del humano

Gap de producto confirmado por implementador y revisor: `actions.closeDocument()` solo se invoca desde `PasswordDialog` (al cancelar) y desde el banner de error fatal de `PipelineStatus` (visible solo con `stage === Failed`). Un documento que llega a `Ready` no se puede cerrar desde la UI — la única salida es recargar la pestaña —, y `validateImportInput` exige ese cierre para importar el siguiente. Contradice el supuesto de ADR-048 §3 (el Escenario 7 era "ejercitable con lo que ya existe") y choca de frente con el gate `test:leak` de Hito 11 (10 ciclos open/close).

**Decisión — ADR-051, ratificada por el humano (2026-07-30)**: `CloseDocumentButton` nuevo en el `Toolbar`, visible con `stage ∈ {Ready, Done, Failed, Cancelled}` (con el pipeline corriendo el control es `CancelButton`), **con `ConfirmDialog`** porque cerrar descarta ediciones y reglas sin undo — misma convención que `CancelButton` y "Eliminar grupo". El "Cerrar documento" del banner de `Failed` se conserva **sin** confirmación (ahí no hay nada que perder) y queda documentado que los dos conviven a propósito. Aplica la regla 9 de `Components.md` §13 (el gate por `stage` condiciona el botón, no la vida del diálogo abierto). Cero cambios en `packages/`: `closeDocument` ya existe y hace lo correcto. **PR 17.7** (`apps/react-client`), con el des-`fixme` del Escenario 7 como evidencia. Descartadas: cerrar sin confirmación; reinterpretar el escenario sobre el banner de `Failed` (`corrupt.pdf` muere en `Extracting`, el ciclo no ejercita nada de lo que hay que ver liberado); diferir a v1.0.

**Implementado (2026-07-30)**: `CloseDocumentButton.tsx` nuevo (hermano de `CancelButton`/`ExportButton`) + `closeDocumentButtonVisibility.ts` (lógica pura, mismo patrón que `exportButtonVisibility.ts` de PR10) + montaje en `Toolbar.tsx`. Regla 9 aplicada idéntica a `ExportButton.tsx` (comparado línea por línea). `variant="ghost"` en el trigger (no `secondary` como `CancelButton`) para no confundirse visualmente, decisión de criterio del implementador dentro del espíritu del ADR. 22 tests unitarios de visibilidad. `tests/e2e/support/blobUrlTracker.ts` nuevo (instrumenta `URL.createObjectURL`/`revokeObjectURL` vía `addInitScript`, sin tocar `apps/react-client/src`). Escenario 7 E2E sacado de `fixme`: 10 ciclos importar→`Ready`→cerrar, verificando estado limpio por ciclo y que tras cerrar se puede importar otro documento sin recargar.

**Revisor: APPROVED.** Confirmó cero cambios en `packages/` y que `PipelineStatus.tsx`/`CancelButton.tsx`/`actions.ts` no fueron tocados; comparó la regla 9 literalmente contra `ExportButton.tsx`; corrió el Escenario 7 cuatro veces con `--workers=1` (config real de CI), siempre verde en ~4s, **sin reproducir** el "1 blob URL vivo" que el implementador había visto bajo la carga de 6 workers en paralelo del sandbox — coincidió en que el trade-off de `mediatedPreviewCtx`/`abortRegistry` (ADR-038 §6) que el implementador señaló como causa tentativa es ajeno a este PR y está bien reportado aparte (chip `task_bd85c1c8`) sin tocar `packages/`.

**Gates finales del lote completo (17.4+17.5+17.6+17.7), corridos de primera mano por el revisor**: `pnpm lint` verde (10m37s), `pnpm typecheck` verde, `pnpm test` 907 tests/75 archivos, `pnpm test:contract` 203/10, `pnpm test:coverage` sin fallos de threshold (`render.engine.ts` 94.93%, `orchestrator.ts` 91.49%, `ocr-engine/worker/kernel.ts` 96.98%). `pnpm test:e2e --workers=1` (config real de CI): **12/12 verde**, Escenarios 2/3/7 corridos 3-4 veces cada uno, siempre verdes.

**Otras observaciones no bloqueantes del revisor**:
- `tests/e2e/scenario-3-protected-pdf.spec.ts` usa `getByRole("button", {name: "Cerrar documento"})` sin scope como proxy de "no hay banner de error" — desde PR17.7 ese nombre accesible ya no es exclusivo del banner (ahora también lo tiene el botón del Toolbar). Hoy no da falso positivo porque los checkpoints son mid-pipeline (el botón del toolbar está oculto), pero `scenario-6-corrupt-pdf.spec.ts` ya usa la forma robusta con scope — convendría alinear scenario-3 en un PR futuro.
- `OCR_Engine.md` §14 no lista los dos tests nuevos de `worker-entry.test.ts` (a diferencia de `Render_Engine.md`/`Orchestrator.md`, que sí listan los suyos) — trabajo del planificador, R-21 se lo prohíbe al implementador.
- `Orchestrator.md` §2 y `06_Pipeline.md` §73/§196 siguen describiendo `loadDocument(documentId, buffer)` en prosa (dos argumentos) — no incorrecto (el tercero es opcional), pero quedaron desactualizados en la forma.

**Estado dejado**: los cuatro PRs (17.4, 17.5, 17.6, 17.7) cerrados, revisor APPROVED en un solo pase. Working tree sin commitear en `feat/hito10-react-client`, a la espera de autorización explícita del humano para `git commit`.

---

## Bug destapado durante PR17.7 — blob URLs que sobreviven al cierre del documento (ADR-052; cerrado, revisor: APPROVED)

**Encontrado por el implementador** al escribir el Escenario 7 (10 ciclos import→`Ready`→close con `blobUrlTracker`), y reportado **sin arreglar ad hoc**, correctamente: la hipótesis tocaba invariantes de tres ADRs.

Lo reportado: fallas intermitentes de exactamente 1 blob URL vivo tras el cierre, solo con paralelismo 6 (ciclo 7 en una corrida, ciclo 3 en otra), verde en serie y en el modo real de CI (`playwright.config.ts` fija `workers: 1`); una de las corridas paralelas además volteó el dev server con `ENOMEM`. El propio implementador marcó la atribución como lead, no como causa confirmada — bien hecho, y el planificador coincide: **el flake no es la evidencia**, el defecto se arregla por sus méritos y la regresión tiene que ser un test determinista, no un E2E en un entorno sin memoria.

Verificado por el planificador contra el código: la ventana **existe por construcción** —`handlePreviewUpdated` hace `blobTracker.set` incondicional, y tras el `revokeByPrefix` de `closeDocument` ningún cierre futuro vuelve a barrer ese `documentId`— y es **más ancha que la hipótesis**. Fuentes de llegada tardía: (1) el preview mediado, cuyo `AbortController` de la v1.5.1 nunca se aborta; (2) **la vía por evento de Render** — `handleRenderRequested` usa el `ctx` de `init()`, no el del documento, así que **ningún** render de `RENDER_REQUESTED` es cancelable con `abortRegistry.abort(documentId)`, y el Escenario 7 emite `RENDER_REQUESTED` en cada ciclo; (3) `handleExportFinished`, misma forma. O sea: una decisión que solo tocara `mediatedPreviewCtx` no habría cerrado el agujero.

Corrección a una de las dos opciones que planteaba el reporte: **"que el tracker se niegue a registrar" garantiza el leak en vez de arreglarlo**. El URL ya lo creó el motor (ADR-034 §5: crear es del motor, revocar es del Orchestrator); si el Orchestrator no lo toma, nadie lo libera nunca.

**Decisión — ADR-052**: (a) guard en `handlePreviewUpdated`/`handleExportFinished` que ante un `documentId` fuera de `state` **revoca el URL entrante en el acto** + `warn` — es el que cierra el agujero, y cubre las tres fuentes; (b) `mediatedPreviewCtx` pasa a un `AbortController` por documento que `closeDocument`/`dispose` abortan y `cancelReanalyze` **no**, refinando ADR-044 §3 y la nota v1.5.1 a "inmune a la **cancelación**, no a la **baja**" (ADR-038 §6 intacto). (b) sin (a) no arregla nada; (a) sin (b) deja renders zombie. Sin cambios de contrato ni de payloads; `render-engine` no se toca. **PR 17.8** (`packages/anonymization-core/src`). Docs: `Orchestrator.md` v1.5.4 (§13 caso 11, §14 cuatro tests, §15.21), `Render_Engine.md` §8 (precisión sobre la cancelabilidad de la vía por evento), notas de amendment en `ADR-034` §5 y `ADR-044` §3.

**Implementado (2026-07-31)**: `orchestrator.ts` — guard `if (!this.state.has(documentId))` en `handlePreviewUpdated`/`handleExportFinished`: revoca el URL entrante + `logger.warn` + no registra. `mediatedPreviewControllers` nuevo (mapa por documento), abortado/borrado en `closeDocument`/`dispose`, **no tocado por `cancelReanalyze`**. Cuatro tests nuevos con nombres exactos de `Orchestrator.md` §14 en `edge.test.ts` (evento tardío de preview, evento tardío de export, evento durante el `await unloadDocument` — que sí se registra y barre normal —, y el de no-regresión de `cancelReanalyze`). Un test preexistente de `unit.test.ts` ajustado (agregó el `importDocument` previo que el guard nuevo exige para no disparar en el primer emit) sin cambiar lo que prueba.

**Revisor: APPROVED.** Verificó línea por línea el orden de `closeDocument` (sin `await` entre `revokeByPrefix` y `state.delete`, la propiedad que hace determinista el guard); que `cancelReanalyze` no tiene ninguna referencia a `mediatedPreviewControllers` (grep + lectura completa del método); que los dos call sites de `mediatedPreviewCtx` están guardados contra documento cerrado, sin posibilidad de recrear un controlador huérfano; y examinó el test 3 en detalle para confirmar que sí ejercita la ventana "durante" (no "después") y que el guard no se adelanta. Gates: `pnpm lint`/`typecheck` verdes, `pnpm test` 911/75, `pnpm test:contract` 203/10, cobertura `orchestrator.ts` 91.69% líneas.

**Observaciones no bloqueantes del revisor, sin acción tomada**:
- El test 3 emite el evento tardío de forma síncrona dentro del mock de `unloadDocument`, sin un `await Promise.resolve()` que ejercite un interleave asincrónico genuino — cae dentro de la ventana correcta igual, mejora opcional.
- La determinismo del guard depende de que nunca se inserte un `await` entre `revokeByPrefix` y `state.delete` en `closeDocument` — hoy se cumple y está comentado, pero ningún test detectaría una regresión futura que rompiera esa propiedad. Candidato a nota de spec o test de orden en un PR aparte.
- `URL.revokeObjectURL` ahora tiene dos call sites directos en `orchestrator.ts` además de `BlobUrlTracker` — es literalmente lo que pide el ADR, no un desvío; si aparece un tercer caso, un `BlobUrlTracker.revokeUntracked()` centralizaría.

**Estado dejado**: PR17.8 cerrado. Gates verdes de primera mano. Working tree sin commitear en `feat/hito10-react-client`, a la espera de autorización explícita del humano para `git commit`.

---

## Cierre del Hito 10 — cinco hallazgos de prueba manual, abiertos al momento del merge (2026-07-31)

Con el Hito 10 ya cerrado a nivel de PRs, el humano probó la app con **dos documentos propios** (un cuento de 11 páginas con texto real, y una pericia legal escaneada+texto) y reportó tres síntomas. El planificador los investigó contra el código antes de asignar nada (regla de ambigüedad: no improvisar diseño), y en el camino aparecieron dos hallazgos más. **Ninguno se arregló todavía**: se mergea el Hito 10 con los cinco documentados acá, y cada uno arranca desde su propia rama sobre `main`.

Lo que sigue es diagnóstico verificado contra el código, no hipótesis, salvo donde se aclara explícitamente.

> **Actualización 2026-07-31 — los tres ADRs están escritos y los specs enmendados.** `ADR-053` (pdf.js dentro de un Worker), `ADR-054` (scroll independiente por panel) y `ADR-055` (decodificación del resultado que cruza un Worker), más las enmiendas a `Render_Engine.md` v1.7.0, `PDF_Engine.md` v1.3.1, `NER_Engine.md` v1.2.1, `05_Worker_Architecture.md` §2.2/§7, `React_Client.md` §3.5/§3.6/§7, `Components.md` §5.1/§5.3/§5.6, `07_Performance_Strategy.md` §3.1, `Code_Standards.md` §7 y notas de amendment en `ADR-018` y `ADR-042`. La tabla de PRs está en `MVP.md`, al final del Hito 10. Nada implementado todavía.
>
> **Hallazgo del propio trabajo de ADR-053**: al verificar la solución contra el código publicado de `pdfjs-dist@4.10.38` aparecieron **tres trampas** que habrían roto el fix, una de ellas catastrófica —pasar `cMapUrl`/`standardFontDataUrl` sin fijar `useWorkerFetch: false` hace que pdf.js evalúe `document.baseURI` dentro del Worker y tire `ReferenceError`, que `kernelLoadDocument` reclasifica como `RenderFailedError`: el visor entero deja de cargar documentos, con un error que no menciona fuentes—. Están en ADR-053, Contexto §6, con número de línea. Es el tipo de verificación que justifica escribir el ADR antes que el código.

### 1. Crítico — NER no detecta absolutamente nada con `NerPool` real

**Síntoma**: desde que los motores pasaron a Workers reales, al escanear un PDF de texto solo aparecen las entidades de Regex (una patente). Ninguna de NER (personas, ubicaciones) llega nunca a la UI, sin error visible, con el pipeline llegando a `Ready` normalmente.

**Causa, confirmada**: desajuste de sobre entre el worker y el host. `ner-engine/src/worker/entry.ts:104` postea `COMPLETED` con `result: { spans }` — que es exactamente lo que manda ADR-046 §1 y lo que documenta el comentario de `shared/src/types.ts` sobre `NerKernelSpan`. Pero `ner.engine.ts:526` tipa el dispatch como `ReadonlyArray<NerKernelSpan>` y en la línea 535 itera el resultado directo, y `WorkerPool.dispatch` resuelve con `outbound.result as TResult` (`worker-pool.ts:662`, cast a ciegas sobre un valor que cruzó un `postMessage`). En modo remoto llega el objeto `{ spans }` → `for...of` sobre algo no iterable → `TypeError`. Ese error no es un `EngineError`, así que `normalizeNerError` lo deja pasar tal cual, no es `NerTimeoutError` → no reintenta → `NerPageFailedError` → `processPages` lo traga con `ctx.logger.warn` (`ner.engine.ts:423`), y el logger de producción es el nulo (P-4). Once páginas fallando en silencio, `NER_FINISHED` con `occurrenceCount: 0`.

Los otros cuatro motores no lo tienen: OCR/PDF/Render/Export postean `result` pelado. NER es el único que envuelve.

**Por qué pasó todos los gates — y este es el hallazgo más importante de los cinco**: no existe **ningún** test, en ningún nivel, que verifique que una entidad detectada por NER llegue a la UI. Los unit de `ner-engine` usan pools fake que ejecutan `run()`, o sea el camino in-process, que devuelve el array pelado y nunca cruza el sobre. `worker/__tests__/entry.test.ts:291` assertea `expect(viaWorker).toEqual({ spans: viaDirect })` — testea el sobre y lo da por bueno, sin nadie del otro lado que lo abra. El E2E tampoco: el Escenario 1 dice textualmente que assertea un DNI de Regex "sin depender de NER"; los Escenarios 5 y 9 asserean sobre `NER_MODEL_LOADING` (que viaja por `PROGRESS` y sí funciona); el Escenario 8 assertea **ausencia** de entidades NER, y por lo tanto pasa igual de verde con el bug presente.

**Decisión — ADR-055 primero (pendiente de escribir), fix de `ner-engine` como su primer adoptante.** El humano eligió explícitamente escribir el ADR antes que el código. El ADR no puede limitarse a enunciar el principio ("decodificá con un guard"): tiene que mandar un mecanismo, porque un principio no impide que el próximo `dispatch<Foo>()` vuelva a mentir. El mecanismo elegido: **cada motor angosta su propio puerto interno** (`NerJobPool`, `OcrJobPool`, `RenderJobPool` — todos viven dentro del archivo de su motor) para que devuelva `unknown`, de modo que el compilador obligue a decodificar. Como esos puertos son per-motor, no hay que tocar `worker-pool.ts` ni el façade, y cada motor se migra en su propio PR sin violar R-1/R-5. El ADR debe exigir además, por motor, un test que cruce el sobre real (pool fake que ignore `run()` y resuelva exactamente lo que postea su `entry.ts`).

Los otros cuatro motores **no** tienen el bug hoy; su migración al puerto `unknown` es una serie de endurecimiento posterior, sin urgencia.

### 2. Texto reconstruido como cuadrados (`.notdef`) en preview y export

**Síntoma**: en una pericia legal real, todos los caracteres se dibujan como cuadrados de borde negro y relleno blanco, tanto en el panel original como en el anonimizado y en el PDF exportado. Único workaround que encontró el humano: pasar el PDF por un conversor online a `.docx` y de vuelta a PDF.

**Diagnóstico, con la evidencia que lo cerró**:

- La misma pericia **se renderiza perfecta en Firefox**, que usa pdf.js con toda su configuración por defecto. O sea: el PDF está sano, el problema es de configuración nuestra.
- La consola de la app con el documento cargado **no emite ni un warning de fuentes**: ni `standardFontDataUrl`, ni `Unknown CMap name`, ni `Cannot load system font`. pdf.js avisa fuerte cuando le faltan esos assets, y no avisó — **las fuentes de ese documento están embebidas y no falta ningún asset**. Esto descartó la primera hipótesis del planificador (que el fix fuera servir `cmaps/`/`standard_fonts/`), y dejó como causa la única que falla en silencio.

**Causa**: el kernel de render corre dentro del RenderWorker, donde `globalThis.document` no existe. pdf.js intenta registrar cada fuente embebida con un `@font-face`: `FontLoader.isFontLoadingAPISupported` da `false` (`pdf.mjs:5391`, es `!!this._document?.fonts`) y cae a `insertRule`, que hace `this._document.createElement("style")` (`pdf.mjs:5310`) → `TypeError`. Ese error lo captura un `.catch()` que **no loguea nada** (`pdf.mjs`, handler del commonobj `"Font"`) y dispara el fallback interno. Del lado del canvas, `disableFontFace` sigue en `false` — solo se auto-activa bajo Node (`pdf.mjs:11376`, `isNodeJS`), nunca en un Worker de browser — así que pdf.js dibuja con `ctx.fillText()` pasando códigos del área de uso privado de una fuente que nunca se registró. El navegador no tiene glifo para esos códigos: cuadrados. Silencioso de punta a punta, que es exactamente por qué el log está limpio.

Explica también las dos anomalías que el humano observó en ese documento: **la página 1 se reconstruye bien porque es una imagen escaneada** (no tiene fuentes que registrar; su "fuente y tamaño distintos" era, en realidad, ausencia de fuentes), y las páginas 2+ tienen texto real con fuentes embebidas → cuadrados.

**Decisión (ADR-053, pendiente de escribir)**, con las dos mitades que el humano eligió tomar juntas por robustez:

- **(a) La que arregla el síntoma**: `disableFontFace: true` (+ `useSystemFonts: false` explícito) en `kernelLoadDocument`. pdf.js pasa a pedirle al worker las siluetas de glifo (`pdf.worker.mjs:30963` construye los commonobj `${loadedName}_path_${fontChar}`) y las dibuja como `Path2D` — sin DOM, correcto por construcción dentro de un Worker. Arregla preview y export, porque el export compone el mismo raster. Costo conocido: dibujar por silueta es más lento que `fillText`; hay que medirlo y reportarlo, no optimizarlo en ese PR.
- **(b) La que previene el caso que hoy no tenemos**: servir `cmaps/` (169 archivos, 1.5 MB) y `standard_fonts/` (804 KB) de `pdfjs-dist` como assets first-party, y pasar `cMapUrl`/`cMapPacked`/`standardFontDataUrl` tanto en `render-engine` (rasterizado) como en `pdf-engine` (extracción de texto — sin cMaps, un PDF con CMap predefinido se extrae mal y degrada la detección de entidades). Cubre las fuentes **no embebidas** y las CID con CMap predefinido, que (a) no toca. Los bytes salen de una dependencia npm ya pinneada por `pnpm-lock.yaml`, así que **no** van a `assets.lock.json` (que es para mirrors de CDN): se copian desde `node_modules` en un paso de build, y `apps/react-client/public/pdfjs/` se agrega al `.gitignore` junto a `public/wasm/` y `public/models/`. pdf.js pide solo el archivo puntual que necesita, así que un documento que no los necesita no paga nada en runtime.

Toca dos motores + la app, así que se parte en: PR de app (assets), PR `render-engine`, PR `pdf-engine` (R-1/R-5). El PR de app va primero: sin assets servidos, los otros dos no tienen a qué apuntar. La verificación manual con la pericia real es gate obligatorio del PR de `render-engine`.

### 3. pdf.js degrada a "fake worker" dentro de todo Web Worker

**Síntoma**: la consola emite `Warning: Setting up fake worker` cuatro veces (una por el hilo principal, tres desde `kernel.ts:289`, o sea un RenderWorker por slot).

**Causa, confirmada leyendo el código publicado**: no es un olvido nuestro — `render-engine/src/worker/entry.ts:71` y `pdf-engine/src/worker/entry.ts:73` configuran `GlobalWorkerOptions.workerSrc` correctamente, cada uno en su scope. Es una limitación de pdf.js 4.10.38: `PDFWorker._initialize()` hace `PDFWorker._isSameOrigin(window.location.href, workerSrc)` (`pdf.mjs:12309`) y **`window` no existe dentro de un Web Worker** → `ReferenceError` → lo captura y llama `_setupFakeWorker()`. O sea, **pdf.js nunca puede spawnear su worker real desde adentro de un worker**, hagamos lo que hagamos con `workerSrc`.

**Consecuencia: de rendimiento, no de corrección.** El parser de pdf.js corre en el mismo hilo del RenderWorker en vez de en uno propio (el camino de mensajes es el mismo, sobre un `LoopbackPort`). No causa el bug 2 ni ningún otro síntoma observado. Existe un workaround —definir un `window` mínimo en el scope global del worker antes de importar pdf.js— pero es un monkey-patch sobre una librería de terceros y **no debe aplicarse sin ADR propio**. Queda anotado, sin fix, para evaluarlo cuando el rendimiento de render sea el cuello de botella (candidato natural: Hito 11).

### 4. `net::ERR_FILE_NOT_FOUND` de blob URLs al scrollear

**Síntoma**: al scrollear el visor, la consola tira `GET blob:http://localhost:5173/<uuid> net::ERR_FILE_NOT_FOUND`, una por página.

**Causa**: scrollear dispara `RENDER_REQUESTED` para el rango montado; cuando llega el preview nuevo de una página, `BlobUrlTracker.set` (`blob-tracker.ts:19`) revoca el URL anterior de esa clave en el acto. Si el `<img>` de `PageCanvas` todavía estaba cargando el URL viejo, su carga muere con ese error.

**Impacto**: visualmente inofensivo — `PageCanvas` ya descarta el resultado obsoleto vía su flag `active`, así que el `onerror` no pinta nada. Es ruido de consola que indica que la ventana entre "revoco el viejo" y "el `<img>` viejo terminó" no está cerrada. Misma familia que ADR-052, pero del lado de la UI y sin consecuencia funcional conocida. **Sin fix**; anotado para revisarlo si algún día produce un síntoma visible.

### 5. Una página escaneada dentro de un PDF de texto no recibe cobertura de OCR

**Síntoma**: en la pericia, el nombre del fiscal —que está en el título de la página 1— es la única entidad que la app no detecta, mientras que los nombres de las páginas siguientes sí se detectan bien.

**Causa parcial, confirmada por el humano**: la página 1 **es una imagen escaneada** (no permite seleccionar texto; las páginas 2+ sí). `getTextContent()` no devuelve nada para esa página, así que ni Regex ni NER pueden ver ese nombre. Ninguna configuración de pdf.js lo cambia: ese nombre solo es alcanzable por OCR.

**Lo que queda abierto**: el pipeline debería detectar una página sin texto y mandarla a OCR. O no se está disparando para páginas sueltas dentro de un documento mayormente textual, o corre y no detecta. **No investigado todavía** — es el único de los cinco hallazgos cuya causa no está cerrada. Requiere su propio diagnóstico sobre `Orchestrator`/`OCR_Engine` antes de decidir nada.

### Y la entrada 3 de "Post-PR13" (salto de scroll) deja de estar "no confirmada"

El síntoma volvió a aparecer en prueba manual, con pasos precisos: pantalla lo bastante ancha como para mostrar los dos paneles a la vez, y cruzar de una página a otra en cualquiera de los dos. Resultado: los dos visores saltan a la primera página y el scroll queda trabado.

La hipótesis que había quedado anotada (entradas viejas en el `Set` del `IntersectionObserver`) es correcta en su intuición pero **incompleta**: son tres defectos que se componen, y el del `Set` es solo el primero.

1. `PdfViewer.tsx:141` hace `setPage(range.start)`, y `range.start` sale de `computeVisibleRangeFromIndices` (`visibleRange.ts:23`), que es literalmente `min(set)..max(set)`. Basta con que el `Set` contenga transitoriamente un índice viejo —esperable cuando el otro visor se desplaza miles de píxeles de golpe y las entradas de entrada y de salida caen en callbacks distintos, coalescidos por el `requestAnimationFrame` de `PageVirtualizer.tsx:108`— para que el rango colapse a `start: 0` y `setPage(0)` mande **los dos** visores al principio. `min..max` no tolera un `Set` no contiguo ni por un frame.
2. No hay dueño del scroll: los dos visores son a la vez emisor y receptor. Cuando A scrollea, el efecto de `PageVirtualizer.tsx:70-82` desplaza B; B dispara su propio observer y **vuelve a escribir `currentPageIndex`**, lo que puede arrastrar a A. `computeScrollSyncTarget` corta la recursión infinita pero no impide que el seguidor mueva al líder.
3. La sincronización es por índice de página, no por píxel (`scrollSync.ts:49`: `targetPageIndex * pageSize`): el seguidor queda alineado al borde de una página mientras el líder está en un offset arbitrario. Es lo que ya estaba anotado como limitación aceptada más arriba ("hasta ~1 página de diferencia"), pero es también lo que convierte el defecto 2 en un ping-pong estable — y cerca del final del documento el navegador recorta el `scrollTop` del seguidor, que entonces reporta siempre una página menos que el líder: eso es el "no me deja bajar".

Que el implementador no pudiera reproducirlo en Playwright headless es coherente: los tres defectos dependen de timing de entrega del `IntersectionObserver`, que en headless es distinto.

**Decisión del humano (ADR-054, pendiente de escribir)**: **scroll independiente por panel, más un control de "sincronizar scroll" que el usuario prende cuando quiere.** Con el control apagado —el estado por defecto— no hay ningún mecanismo de sincronización, así que los tres defectos desaparecen por eliminación, no por arreglo: se borran `scrollSync.ts` y la prop `scrollToPageIndex`, nadie mueve a nadie por código, y el `min(Set)` deja de ser peligroso porque solo decide qué páginas montar (equivocarse ahí significa montar una página de más, inofensivo).

Implica que `viewer.store.visibleRange` y `currentPageIndex` pasan a ser **por panel** en vez de globales. Verificado: fuera del propio visor, el único consumidor externo es `SettingsDialog.tsx:159` (lee `visibleRange` para re-pedir previews al cambiar un setting), que pasaría a usar la unión de los dos rangos.

Con el control prendido, la sincronización **no** puede reimplementarse por índice de página: tiene que ser a nivel de píxel (`seguidor.scrollTop = líder.scrollTop`, geometría idéntica entre paneles), de modo que el evento de scroll que genera el seguidor calcule un valor **ya igual** al compartido y no escriba nada. La convergencia sale de la idempotencia, no de suprimir eventos con banderas ni timeouts — cualquier diseño con una ventana temporal reintroduce un bug dependiente de timing como el actual. Casos límite a resolver de forma determinista en el ADR: panel que no puede alcanzar la posición (recorte del navegador cerca del final) no debe escribir su posición recortada, y panel oculto (alto cero) se saltea y se re-sincroniza al volver a mostrarse.

**Restricción explícita del humano**: hay que preservar el comportamiento actual en anchos chicos (`< lg`), donde `SideBySideViewer` muestra pestañas para elegir qué documento previsualizar. El ADR tiene que definir qué hace el control de sincronización en ese modo.

**Requiere ADR porque contradice tres specs**: `Components.md` §5.1 ("dos PdfViewer con scroll sincronizado"), `React_Client.md` §7 ("Lado a lado sincronizado: scroll vertical compartido vía `viewer.currentPageIndex`") y `07_Performance_Strategy.md` §3.1 ("dos virtualizers sincronizados vía estado Zustand"). También hay que decidir ahí si el flag nuevo reutiliza `viewer.store.sideBySide` —declarado en `React_Client.md` §3.5, hoy sin setter y sin consumidor, ya anotado como ambigüedad en `SideBySideViewer.tsx:20`— o si se agrega un campo propio; el planificador se inclina por un campo propio, para no darle a un campo documentado un significado que no tiene.

---

## Gate `test` de CI — la suite unitaria dependía de assets no commiteados (detectado y corregido en el PR del Hito 10)

Al abrir el PR del Hito 10, el job `test` (`pnpm test -- --coverage`) falló por primera vez: **904 tests pasaron y 2 suites no llegaron a cargar** — `apps/react-client/src/__tests__/core-adapter.test.ts` y `core-adapter-init-precedence.test.ts`, ambas con `Error: Denied ID ../assets/onnxruntime/ort-wasm-simd-threaded.asyncify.mjs?url`. Falla de colección, no de aserción.

**Causa**: `core-adapter/index.ts` importa los dos assets de onnxruntime con `?url` (ADR-039 §3), y esos archivos viven en `apps/react-client/src/assets/onnxruntime/`, que está en `.gitignore` — los produce `pnpm assets:mirror`. En las máquinas de desarrollo existen (el mirror se corrió alguna vez), así que el gate pasaba local; en una checkout limpia de CI no existen y Vite rechaza el id al cargar cualquier suite que importe el `core-adapter`.

Esto **estaba parcialmente anotado** más abajo, en la tarea de seguimiento sobre `assets:mirror` (ADR-048 §1), pero solo para el job `e2e` — cuyo paso de mirror + cache sí existe en `ci.yml` desde PR17, con un comentario que nombra exactamente este modo de falla. Lo que nadie había notado es que **el job `test` tiene la misma dependencia**. No es una regresión del Hito 10: es la primera vez que CI corre sobre esta rama.

**Fix aplicado (no se copió el paso de mirror al job `test`)**: mirrorear para la suite unitaria significa bajar 219 MB —de los cuales 178 MB son el modelo NER que ninguna suite unitaria carga— para resolver dos imports, y ataría el gate más caliente del repo a una descarga de red. En su lugar, `vitest.config.ts` gana un plugin `anonly:ort-url-asset-stub` (`enforce: "pre"`) que intercepta esos dos ids antes que `vite:resolve` y los sirve como módulo virtual con una URL de juguete. Es todo lo que las suites necesitan: la única aserción sobre esos valores es `expect(receivedConfig?.ner?.wasmPaths).toBeDefined()` — les importa que la inyección de ADR-039 ocurra, no qué URL produjo el bundler (que en un build real es un nombre hasheado, no asertable).

Alcance: solo `vitest.config.ts`, o sea solo `vitest`. El build de la app y el job `e2e` siguen usando los archivos reales, así que el camino de producción no queda sin cubrir.

**Verificado localmente** moviendo `src/assets/onnxruntime/` fuera del árbol para reproducir la checkout limpia: las dos suites cargan y pasan. Con los assets presentes, `pnpm test -- --coverage` completo da 75/75 suites, 911/911 tests, thresholds de cobertura verdes.

### Y el gate `e2e`: el Escenario 11 asumía un presupuesto de tiempo fijo

Con el job `test` ya en verde, la primera corrida real del job `e2e` dio 11 de 12 escenarios verdes. El que falló —`scenario-11-zoom.spec.ts`, "cambiar el zoom re-escala de inmediato y reemplaza el bitmap transitorio"— falló las tres veces (inicial + los 2 reintentos de `playwright.config.ts`), o sea que no era un flake marginal.

**Causa**: el test capturaba el `toDataURL()` del canvas, esperaba `page.waitForTimeout(800)` y assertaba que el bitmap hubiera cambiado. Esos 800 ms tenían que cubrir `ZOOM_RERENDER_DEBOUNCE_MS` (150 ms) **más** un ciclo completo `RENDER_REQUESTED` → RenderWorker → `PREVIEW_UPDATED` → blob → `<img>` → `drawImage`, con 3 páginas × 2 kinds en vuelo. Alcanza en una máquina de desarrollo; no en un runner de CI de 2 cores, con Chromium rasterizando por software y pdf.js degradado a "fake worker" dentro del RenderWorker (hallazgo 3 de arriba: parsea en el mismo hilo que rasteriza). Era además **el único presupuesto fijo de la suite** para esperar un render: el Escenario 3 y el 7 ya usaban `expect.poll` con 30 s y 15 s.

**Fix**: `expect.poll(() => canvasDataUrl(canvas), { timeout: 20_000 }).not.toBe(transitionalBitmap)` — se espera por la condición, no por una duración. No afloja la prueba: si el re-render de verdad no ocurriera, sigue fallando, solo que tras agotar el presupuesto, y ahí sí sería un bug de producto. Sin tocar código de producto.

**Nota de método**: la verificación local de este fix no prueba nada (localmente el test ya pasaba con los 800 ms); la única verificación válida es la corrida de CI.

---

## PR A1/A2 — ADR-055: decode del sobre NER (`ner-engine`) + E2E de entidad en el panel (`tests/e2e`)

Dos PRs en la misma branch (`fix/ner-engine-worker-envelope-decode`), cerrando el bug 1 del cierre de Hito 10 (NER no detecta nada en producción, ver entrada "PR10" arriba) y el agujero de testing que lo dejó pasar (ADR-055 Contexto §2). En curso al momento de esta entrada — PR A1 en su tercera ronda tras un rechazo del revisor; PR A2 ya aprobado.

**PR A2 (`tests/e2e`) — revisor: APPROVED**, gates propios de primera mano (`pnpm lint` completo, `pnpm typecheck`, `pnpm test`, `pnpm test:contract`, `pnpm test:coverage`, Playwright de los dos specs tocados). Observaciones no bloqueantes, sin acción tomada:
- `wrapText` y seis constantes de layout en `tests/e2e/support/fixtures.ts` están **copiadas** de `tests/fixtures/generate.ts`, mientras que `TEXT_10P_PAGES` sí se importa de ese mismo módulo — el borde de import ya existe, así que exportar también `wrapText`/las constantes hubiera sido más chico que duplicarlas. Tal como quedó, un cambio de layout en `generate.ts` puede desincronizar `textTenPagesWithPersonFile` sin que nada falle.
- `scenario-5-edit-during-ner.spec.ts` ahora prueba dos contratos a la vez (preservación de edición durante NER + ADR-055 §6, entidad visible). Si la aserción dependiente del modelo real se vuelve inestable en el futuro, va a poner en rojo un test cuyo nombre promete otra cosa. ADR-055 §6 ya prescribe el reemplazo para ese caso ("bajarla un nivel", test de integración a nivel façade con worker stub) — tenerlo a mano en vez de tocar la aserción actual si eso pasa.
- Docblock de `scenario-5-edit-during-ner.spec.ts` (líneas ~56-59) sigue nombrando `text-10p.pdf`; el fixture real ahora es `text-10p-person.pdf`. Cosmético.
- Los "240s vs. 480s" que compara el docblock del escenario elegido son presupuestos de `test.setTimeout`, no tiempos medidos — la corrida real del revisor dio ~13.8s (escenario 5) y ~14.7s (escenario 9). El argumento de elegir el más barato se sostiene; los números citados no son runtimes.

**PR A1 (`ner-engine`) — revisor: REJECTED en su segunda ronda**, con un hallazgo real (no solo de estilo):

- **Regresión de comportamiento no documentada, sin test**: la solución de la ronda 2 (que corrigió correctamente los tres hallazgos del rechazo anterior — swallow silencioso en `processPages`, test que no ejercitaba `processPages`, `instanceof` de subclase prohibido por Code_Standards §7/ADR-049) hizo que `processPages` aborte el batch completo ante **cualquier** `InvalidInputError`, no solo los que vienen de `decodeKernelSpans`. `processPage` (`ner.engine.ts`) tiene dos guards preexistentes que también lanzan `InvalidInputError` genuino, **antes** del loop de retry — `input == null` y `pageIndex < 0` — que antes de esta ronda cansaban en el warn+continue por página (comportamiento documentado en `docs/core/NER_Engine.md` §9 como restricción de runtime, no de tipos). El comentario nuevo que afirma "un InvalidInputError acá solo puede venir de decodeKernelSpans" es fácticamente falso contra el propio código. Sin test que cubra esos dos caminos vía `processPages` — ninguno existía antes de esta ronda tampoco. Pendiente: acotar el abort a la falla de decodificación específicamente (sin reintroducir el `instanceof` prohibido), agregar el test de regresión que falta, y corregir el comentario.
- Trivial, en el mismo bloque: el docstring de `NerDispatchDecodeFailure` dice que el campo se llama `cause`; el campo real es `decodeError`.

Observaciones no bloqueantes de PR A1, sin acción tomada:
- **`NerDispatchDecodeFailure` confirmado seguro** contra Code_Standards §7 (se construye y se captura en un solo archivo, nunca escapa a la API pública) — pero es un patrón nuevo de manejo de errores sin ADR propio (R-18). El revisor recomienda enmendar ADR-055 para dejarlo escrito antes de que los otros cuatro motores (ocr/render/pdf/export, serie de endurecimiento pendiente de la entrada "PR10"/ADR-055 §7) lo copien — hoy solo vive como comentario en `ner.engine.ts`.
- Los tres tests obligatorios de `NER_Engine.md` §14 tienen los nombres exactos y el revisor confirmó por trazado estático que los tres fallarían contra el código pre-fix (el caso más fuerte: un string de entrada es iterable en JS, así que pre-fix producía ocurrencias basura en silencio en vez de tirar `TypeError`).
- `isNerKernelSpan` valida `entityType` contra el enum `EntityType` completo, más ancho que los cuatro tipos NER de `NER_Engine.md` §10. Permisivo a propósito en un decoder, solo trazabilidad.

---

## Tareas de seguimiento con entrada formal en el tasklist de la sesión

- ~~Diseñar el fix del supersede de render (leak de `pendingRenders` + clave sin `mode`) — bloquea que el PR9 (Export) se dé por completo.~~ **Resuelto 2026-07-19** — ver "Resolución" en la entrada "PR4" arriba.
- ~~Diseñar cómo `ner-engine` expone `wasmPaths` configurable desde la app (bug #3 de la entrada "PR10" arriba) — bloquea que el PR10 se commitee y que el Escenario 1 E2E pase.~~ **Diseño resuelto 2026-07-22 — ADR-039** (`adr/ADR-039-NerConfig-WasmPaths-Overrides-Parciales.md`): la costura es `NerConfig.wasmPaths?: string | NerWasmPaths` (Contracts.md §6, no `CoreRuntimeOptions` — debe viajar serializado al worker en INIT); `createCore` pasa a aceptar `EngineConfigOverrides` (parciales por sección — cierra de paso la raíz del gap "PR5"); los dos assets de ort de `assets.lock.json` cambian su `destination` a `apps/react-client/src/assets/onnxruntime/`. Docs actualizados: `Contracts.md` §3.5/§6, `NER_Engine.md` v1.1.0 (nota ADR-039, §6, casos 16). **Implementado y verificado 2026-07-22** (typecheck/eslint/tests verdes en los 4 paquetes); sin commitear a la espera del bug #6.
- ~~Diseñar el fix del bug #6 (buffer detachment + unhandled rejection en `runExport`) — bloquea el cierre del PR10 y el Escenario 1 E2E.~~ **Diseño resuelto 2026-07-22 — `Orchestrator.md` v1.2.1, sin ADR** (restaura invariantes ya especificadas en §2/§12; ver la resolución en la entrada "PR10" arriba). **Implementado y verificado 2026-07-22** (4 tests nuevos, 57/57 verdes; reproducción directa en Chromium real: import + export de 10 páginas en ~5 s); sin commitear a la espera del bug #7.
- ~~Diseñar el fix del bug #7 (`ExportButton` desmonta el `ExportDialog` abierto al cambiar `stage`) — bloquea el cierre del PR10 y el Escenario 1 E2E.~~ **Diseño resuelto 2026-07-22 — fix de UI puro, sin ADR** (reconciliación de `Components.md` §2.1/§2.5/§13.9 + `React_Client.md` §2.2; ver la resolución en la entrada "PR10" arriba). **Implementado y verificado 2026-07-22** (commit `0e7af1e`, 15 tests nuevos; Escenario 1 E2E completo pasa en ~7.6s).
- ~~Gap latente destapado por el bug #7: tras un export, `stage` queda en `Done` y `reanalyze` post-export se rechaza (`{Ready, Failed}`), o sea que `SettingsDialog` después de exportar falla silenciosamente.~~ **Resuelto 2026-07-22 — ADR-040** (`adr/ADR-040-Done-Equivalente-Operativo-Ready-Reanalyze.md`, opción (b)): `Done` se define como equivalente operativo de `Ready` ("`Ready` con un export completado", informativo, no restrictivo); la precondición de `reanalyze` pasa a `stage ∈ {Ready, Done, Failed}` — el auto-rechazo concurrente se preserva (durante una corrida el stage no es ninguno de los tres). Sin transición `Done → Ready` ni cambio de `PipelineStage`. Docs: `Orchestrator.md` v1.3.0 (§13.21 + 2 tests en §14; corregido a v1.3.1 tras el revisor, ver entrada "PR10"), nota de amendment en ADR-038. **Implementado y verificado 2026-07-22** (commit `0b581d0`, 2 tests nuevos).
- ~~Diseñar el reparto host/worker del estado por documento de `render-engine` (`cache`, `groupOverrides`, `lastAnonymizedInputs`/`lastOriginalInputs`, `pageGroupIndex`, `pendingRenders` — todo lo que el broadcast `load-document` de ADR-036 §4 no cubre) — **bloquea el arranque de PR13** (ADR-041 §5); requiere ADR propio del planificador antes de asignar el PR al implementador.~~ **Resuelto 2026-07-22 — ADR-043** (`adr/ADR-043-RenderEngine-Reparto-Host-Worker-Kernel.md`): la clase `RenderEngine` queda entera host-side (estado, suscripciones, supersede, cache, eventos); el worker corre un kernel sin estado por documento (§7.4) salvo los `PDFDocumentProxy`; todas las vías de render convergen en `RenderPool.dispatch({ payload, run })` (preview incluido — el Orchestrator deja de envolver `rasterizePage`/`renderPage`); `documents` host pasa a `{ buffer, pageCount }`; nuevo control broadcast `unload-document` + `WorkerPool.broadcast` + re-priming de workers nuevos/reemplazados (cierra para el RenderPool la deuda de crash-replace de PR11). Docs: `Render_Engine.md` v1.4.0, `05_Worker_Architecture.md` §7.4, `03_Data_Model.md` §18. **PR13 desbloqueado.**
- Implementar ADR-044 (bug post-PR13: preview anonimizado idéntico al original): mediación `ENTITY_GROUP_*` → `renderPage` en el Orchestrator, retiros en `render-engine`, export de `buildPageReplacements` en `export-engine` — un solo PR (inserción entre PR13 y PR14/OcrWorker; ver la entrada "Bug post-PR13" arriba). **Diseño resuelto 2026-07-23 — ADR-044**; implementación pendiente de asignarse al implementador.
- ~~Resolver el fork bloqueante de PR14 (cómo hacer real el despacho a OcrWorker: motor-en-worker vs. kernel) — bloquea retomar la implementación de PR14.~~ **Resuelto 2026-07-24 — ADR-045** (opción A: `OcrEngine` dueño de su pool host-side + kernel puro; ver la entrada "PR14" arriba). El implementador retoma con `OCR_Engine.md` v1.2.0 §15 items 19–21 como spec de trabajo.
- ~~Alinear los docs de `ner-engine` al patrón host-side + kernel antes de arrancar PR15 (el spec v1.1.0 y `05` §7.3 describían el patrón pre-ADR-043/045, con el motor creando su pool y emitiendo `ENTITY_FOUND` desde el worker) — bloqueaba el arranque de PR15.~~ **Resuelto 2026-07-24 — ADR-046** (tercer espejo; incluye la decisión propia de NER: el ciclo de vida del modelo cruza por `PROGRESS`, no por eventos de dominio, y `worker-pool.ts` gana `DispatchParams.onProgress`). Ver la entrada "PR15" arriba. **PR15 desbloqueado.**
- ~~Decisión del humano (ADR-048 §7 punto 1): Escenario 3 E2E (PDF protegido) — pdf-lib no encripta.~~ **Ratificado 2026-07-24**: `protected.pdf` se commitea, generado una vez con `qpdf`. Lo produce PR17.
- ~~Decisión del humano (ADR-048 §7 punto 2): wiring `settings.store` → `EngineConfig`. Pregunta abierta desde PR10.~~ **Ratificado 2026-07-24**: se crea **PR16.5** (ver su entrada arriba). Pendiente de asignarse; desbloquea el Escenario 8.
- **`pnpm assets:mirror` como prerequisito del gate E2E (ADR-048 §1)**: el job `test-e2e` de CI nunca lo corrió; la suite no puede pasar ahí. Lo aplica PR17 (paso + cache en `ci.yml`); los "gates E2E verdes" de PR10/PR13/PR14 fueron locales. **Ampliación 2026-07-31**: el job `test` tenía la misma dependencia oculta (dos suites del `core-adapter` no cargaban en una checkout limpia) — resuelto sin mirror, con un stub de resolución en `vitest.config.ts`; ver la sección "Gate `test` de CI" arriba.
- Idle-dispose de los pools construidos por el façade (render/ocr/ner/export): al salir de `WorkerPoolManager` perdieron el temporizador de 60 s de `05` §8. Los `Worker` reales se siguen creando perezosamente; lo que falta es la liberación por inactividad. Deuda común a los cuatro, candidata a un PR propio.
- ~~**Implementar ADR-050** (password hasta `RenderEngine.loadDocument` — ver la entrada "Bugs destapados al cerrar el lote de PR17" §1): **PR 17.4** `render-engine` (+ `shared`) y después **PR 17.5** façade.~~ **Cerrado 2026-07-30** — PR17.4/17.5, revisor APPROVED. Escenario 3 E2E verde de punta a punta, incluido preview real.
- ~~**PR 17.6 — rutas de tesseract** (`ocr-engine`, sin ADR).~~ **Cerrado 2026-07-30** — revisor APPROVED. Escenario 2 E2E verde con aserción de entidades reales.
- ~~**Decisión del humano: Escenario 7 — no hay control de UI para cerrar un documento cargado con éxito**~~ **Ratificada 2026-07-30 — ADR-051, cerrado**: `CloseDocumentButton` en el `Toolbar` con `ConfirmDialog`. **PR 17.7**, revisor APPROVED. Escenario 7 E2E verde (10 ciclos). Desbloquea el gate `test:leak` de Hito 11.
- ~~**Implementar ADR-052** (blob URLs tardíos tras cerrar documento — ver la entrada "Bug destapado durante PR17.7"): **PR 17.8**, `packages/anonymization-core/src`.~~ **Cerrado 2026-07-31** — revisor APPROVED. El Escenario 7 y el gate `test:leak` de Hito 11 ahora verifican un invariante que el código de verdad cumple.
- ~~**Implementar ADR-049** (bug destapado en PR17: `PdfPasswordRequiredError` pierde su identidad de clase al cruzar el Worker — ver la entrada de arriba): **PR 17.1** `pdf-engine` (`retryable = false`) y después **PR 17.2** `packages/anonymization-core/src` (guard por `code`, retiro del override `isRetryable`, tests con error deserializado, des-`fixme` del Escenario 3). **Diseño resuelto 2026-07-30 — ADR-049**; implementación pendiente de asignarse al implementador. Bloquea que el Escenario 3 de PR17 salga de `fixme`.~~ **Cerrado 2026-07-30** — PR17.1 (`a55aa51`) y PR17.2 (`65ac29f`), revisor APPROVED. Destapó el bug de ADR-050 (ver arriba).
- Limpieza conjunta de `poolWorkerFactories.render`/`ocr`/`ner` en `orchestrator.ts`: las tres quedan como dead code una vez que cada motor despacha contra su propia pool (ADR-043/045/046). Inofensivo (los pools se crean perezosamente); candidato a un PR de limpieza propio, fuera del alcance de PR15.
- **Escribir ADR-055** (decodificación de `COMPLETED.result` por guard de runtime, con el mecanismo del puerto interno que devuelve `unknown` + test de sobre por motor) y después el **PR de `ner-engine`** que lo adopta primero y arregla el bug 1 del cierre de Hito 10. Prioridad máxima: sin esto, NER no detecta nada en producción. Los otros cuatro motores quedan como serie de endurecimiento posterior, sin urgencia.
- **Escribir ADR-053** (pdf.js dentro de un Worker: `disableFontFace: true` + assets `cmaps`/`standard_fonts` first-party) y después los tres PRs en orden: app (copia de assets + `.gitignore`), `render-engine`, `pdf-engine`. Ver el bug 2 del cierre de Hito 10.
- **Escribir ADR-054** (scroll independiente por panel + control opcional de sincronización a nivel de píxel; estado del visor por-kind; preservar el modo pestañas en anchos chicos) y después el PR sobre `apps/react-client`. Ver la entrada 3 revisada del cierre de Hito 10.
- **Diagnosticar por qué OCR no cubre una página escaneada suelta dentro de un PDF mayormente textual** (bug 5 del cierre de Hito 10, único con causa sin cerrar). Investigación sobre `Orchestrator`/`OCR_Engine` antes de decidir nada.
- pdf.js degrada a "fake worker" dentro de todo Web Worker (`window` inexistente en `PDFWorker._initialize`, bug 3 del cierre de Hito 10). Solo rendimiento. Evaluar el workaround —y su ADR— cuando el render sea cuello de botella; candidato a Hito 11.
- ~~Ruido de `ERR_FILE_NOT_FOUND` de blob URLs revocados mientras el `<img>` anterior seguía cargando (bug 4 del cierre de Hito 10). Sin consecuencia funcional conocida; revisar si alguna vez produce un síntoma visible.~~ **CERRADO (2026-08-05, ADR-056)**: sí producía un síntoma visible, y era el reportado tras ADR-054 — el visor parpadeando al scrollear. La cadena completa: el motor acuña un `URL.createObjectURL` nuevo en **cada** `PREVIEW_UPDATED`, también en aciertos de cache con píxeles idénticos; el Orchestrator revoca el anterior de esa clave (de ahí el `ERR_FILE_NOT_FOUND`); y `PageCanvas`, al ver un `blobUrl` distinto, reasignaba `canvas.width`/`canvas.height`, lo que **borra el bitmap aunque el valor no cambie**. Se arregla en el PR E1 (ADR-056 §5). El re-acuñado del blob URL se conserva deliberadamente (ADR-056 §6: alinear la clave del cache con la del `BlobUrlTracker` es un cambio de ADR-034 §5/ADR-052 con blast radius propio).
- **`tests/e2e/viewer-scroll-jump.spec.ts` ahora falla, no solo "guardia sin disparar"** (encontrado 2026-07-31, durante PR A1/A2 de ADR-055 — `fix/ner-engine-worker-envelope-decode`, sin relación con esos cambios). Este es el mismo spec de la entrada "Post-PR13" arriba (síntoma 3: "un salto grande de scroll aparentaba volver a la página 1"), agregado en su momento como guardia de regresión sin lograr reproducir el síntoma. Al correr la suite completa de `pnpm test:e2e` (12 specs) como parte de la verificación de PR A2, este spec dio rojo — 11/12 verdes, solo este falló. Confirmado con `git stash`/re-run que el fallo es preexistente en la branch base (`main`), no introducido por PR A1/A2 (ninguno de los dos toca `apps/react-client` ni `PageVirtualizer.tsx`). No se investigó la causa ni se intentó ningún fix (fuera de alcance de ambos PRs). **Pendiente**: retomar la hipótesis del `Set` de `IntersectionObserver` sin limpiar en `PageVirtualizer.tsx` ya anotada en "Post-PR13", ahora con la ventaja de tener un repro consistente en vez de un síntoma reportado a mano.
